# 技术原理

## RRF（倒数排名融合）算法原理

### 核心思想

RRF 是一种将多个排序列表合并为单一排序列表的融合算法，无需了解各检索系统的具体评分机制。

### 公式

$$\text{RRF}(d) = \sum_{r \in R} \frac{1}{k + \text{rank}_r(d)}$$

- `d` — 文档
- `R` — 多个排序列表的集合
- `rank_r(d)` — 文档 `d` 在排序列表 `r` 中的名次（从 1 开始）
- `k` — 平滑常数，通常取 **60**（防止头部文档分数过大）

### 直觉理解

| 名次 | 1/(60+rank) |
|------|-------------|
| 1    | 0.01639     |
| 2    | 0.01613     |
| 10   | 0.01429     |
| 100  | 0.00617     |

排名越靠前分数越高，但分数差异被"压扁"——**第1名和第2名的差距远小于线性评分**，这使融合结果对单个系统的排名不过度敏感。

### 典型应用场景（混合检索）

```
关键词检索（BM25）  →  排序列表 A
向量检索（Embedding）→  排序列表 B
         ↓
       RRF 融合
         ↓
     最终排序列表
```

两份列表中都靠前的文档，最终得分最高。

### 关键优势

1. **无需归一化** — 不同检索系统的分数尺度完全不同，RRF 只看名次，天然规避
2. **鲁棒性强** — 某一系统给出离群高分也不会主导结果
3. **实现简单** — 几行代码即可完成
4. **实验证明有效** — 在多数基准测试中优于线性加权融合

### 示例计算

文档 D 在 BM25 中排名第 3，在向量检索中排名第 1：

$$\text{score}(D) = \frac{1}{60+3} + \frac{1}{60+1} = 0.01587 + 0.01639 = 0.03226$$

文档 E 在 BM25 中排名第 1，向量检索中未出现（或排名很靠后）：

$$\text{score}(E) = \frac{1}{60+1} + \frac{1}{60+1000} \approx 0.01639$$

D 的综合得分更高，因为它在两个系统中都表现良好。

---

## 多层次业务缓存设计

### 背景与问题

票据合规审核的核心操作存在两个成本：

| 操作 | 典型耗时 | Token 消耗 | 结果稳定性 |
|------|---------|-----------|-----------|
| 视觉模型识别（Qwen-VL） | 3~8s | 1000~2000 | 同文件完全一致 |
| 意图识别 LLM（qwen-max） | 200~500ms | 200~400 | 相同问题完全一致 |
| RAG 问答（检索+LLM） | 2~5s | 1000~3000 | 相同问题+知识库短期一致 |
| 合规检索（18字段×RAG） | 15~40s | 18000~54000 | 相同票据要素一致 |
| 欺诈检测（5维并行） | 5~15s | — | 相同票据一致（黑名单偶有更新） |
| 全流程审核（12个Agent） | 30~120s | 30000~100000 | 同票据同类型完全一致 |

**核心矛盾**：相同的票据被多次提交审核（例如在系统间流转时），每次都完整跑一遍所有Agent，造成巨大浪费。

### 票据指纹（Bill Fingerprint）

**设计原则**：不能用 `ticket_number` 单字段作为缓存Key——同一票号可能被伪造，要素与真实票据不同。必须基于所有业务要素字段的哈希，保证"所有项都一样"才命中缓存。

```python
# 参与指纹计算的 18 个要素字段（固定顺序，确保哈希确定性）
FINGERPRINT_FIELDS = [
    "ticket_type", "ticket_number", "issue_date", "due_date",
    "amount_numeric", "amount_text", "currency",
    "drawer", "drawer_account", "drawer_bank",
    "acceptor", "payee", "drawee_bank",
    "endorsers",          # list，JSON序列化
    "maturity_days", "trade_purpose", "acceptance_clause", "special_remarks",
]

def compute_bill_fingerprint(bill_element: dict) -> str:
    canonical = json.dumps(
        {k: bill_element.get(k) for k in FINGERPRINT_FIELDS},
        sort_keys=True, ensure_ascii=False, default=str
    )
    return hashlib.sha256(canonical.encode()).hexdigest()[:24]
```

取 SHA-256 前 24 位（96 bit），碰撞概率 < 10⁻²⁸，工程上可忽略。

### 五层缓存架构

```
用户请求
    │
    ▼ 层1：审核任务级缓存（最高层，TTL=4h）
    │  Key: audit_cache:{tenant_id}:{bill_fp}:{task_type}
    │  命中 → 返回历史 task_id，前端直接下载报告
    │  未命中 ↓
    ▼ 层2：各Agent中间结果缓存
    │  ┌─────────────────────────────────────────────────────┐
    │  │  compliance:{tenant_id}:{bill_fp}   TTL=6h          │
    │  │  18字段×RAG检索结果列表（最贵操作）                   │
    │  ├─────────────────────────────────────────────────────┤
    │  │  fraud:{tenant_id}:{bill_fp}        TTL=2h          │
    │  │  五维欺诈评分（印章/重复/篡改/网络/黑名单）            │
    │  └─────────────────────────────────────────────────────┘
    ▼ 层3：RAG 问答缓存（TTL=2h）
    │  Key: rag:{tenant_id}:{query_fp}
    │  query_fp = SHA256(query + bill_context_fingerprint)
    ▼ 层4：意图识别缓存（TTL=24h）
    │  Key: intent:{query_fp}
    │  仅缓存 LLM 通道（关键词通道 < 1ms 无需缓存）
    ▼ 层5：票据图片识别缓存（TTL=24h）
       Key: recognition:{file_md5}
       基于文件内容 MD5，同一物理文件永不重复识别
```

### TTL 设计依据

| 缓存层 | TTL | 理由 |
|--------|-----|------|
| 票据图片识别 | 24h | 同一张物理文件内容不变，识别结果完全确定 |
| 意图识别 | 24h | 相同用户问题的语义意图不会改变 |
| RAG 问答 | 2h | 知识库白天不更新；2h 覆盖大多数重复咨询场景 |
| 合规检索 | 6h | 法规一个工作日内通常不修改；6h 覆盖一个上午或下午的周期 |
| 欺诈检测 | 2h | 黑名单可能有日内更新；2h 平衡实时性与成本 |
| 审核任务 | 4h | 工作日内同张票据的审核结论不变；4h 覆盖半个工作日 |

### 缓存失效策略

当票据发生背书等状态更新时，必须主动失效该票据的中间结果缓存，否则背书后的合规/欺诈判断会从旧缓存读取，导致错误结论。

```python
# 背书完成后调用
await bill_cache.invalidate_bill(tenant_id, old_bill_fingerprint)
# 会删除：compliance:...、fraud:...、audit_cache:...:*（所有业务类型）
# 不删除：recognition:...（文件内容未变）、rag:...（问答与票据状态无关）
```

### 降级保证

所有缓存操作都用 `try/except` 包裹，Redis 不可用时：
- **读操作**返回 `None` → 走完整业务流程（无损降级）
- **写操作**静默跳过 → 下次请求仍走完整流程（无数据丢失）
- 全局开关 `CACHE_ENABLED=false` 可在调试时一键禁用缓存

### 效果估算（单次全流程审核）

| 场景 | 首次（无缓存） | 第二次（部分命中） | 第三次（完全命中） |
|------|-------------|-----------------|-----------------|
| 视觉识别 | 5s / 1500 token | 命中：0ms / 0 | 命中：0ms / 0 |
| 合规检索 | 30s / 36000 token | 命中：< 50ms / 0 | 命中：< 50ms / 0 |
| 欺诈检测 | 10s / — | 命中：< 10ms / 0 | 命中：< 10ms / 0 |
| 整体 | ~120s / ~80000 token | ~80s / ~40000 | **< 100ms / 0** |
