# 票据合规智能审核平台 — 多智能体（Multi-Agent）系统需求设计

> 文档版本：v1.2  
> 编写日期：2024-01-15（v1.2 更新：2024-02-10）  
> 适用范围：汇票、本票、支票三类票据的全流程合规审核 + 跨机构报文流转状态查询 + 欺诈检测 + 批量调度

---

## 一、背景与目标

### 1.1 业务背景

票据经纪业务链路长、参与方多、合规要求严，涵盖出票、承兑、背书转让、贴现、托收等多个环节。传统人工审核面临以下痛点：

| 痛点 | 描述 |
|------|------|
| 效率低 | 单份票据人工审核平均耗时 8 分钟，批量处理瓶颈明显 |
| 专业门槛高 | 审核员需同时掌握人行法规、交易所规则、行内合规手册 |
| 错误率高 | 背书链断裂、要素缺失等低级错误频发 |
| 知识碎片化 | 监管规则更新快，人员培训成本高 |
| 跨部门协作难 | 法务、风控、运营各自为战，审核结论难以统一 |

### 1.2 建设目标

构建以 **Orchestrator + 专项 Agent** 为核心的多智能体协作平台，实现：

- 单份票据端到端审核耗时 ≤ 35 秒
- 批量并发处理 ≥ 100 份/次
- 合规检查覆盖率 ≥ 95%（覆盖已录入法规条款）
- 结构化审核报告支持 JSON / PDF 双格式输出
- 全流程可审计、可溯源

---

## 二、整体架构设计

### 2.1 多智能体总体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            用户交互层                                    │
│         Web Portal  /  API Gateway  /  企业系统对接  /  运营监控大盘      │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────────────┐
│                     主控编排 Agent（Orchestrator）                        │
│           任务分发 · 上下文管理 · 结果聚合 · 异常重试                    │
└──┬─────────┬─────────┬─────────┬─────────┬─────────┬─────────┬──────────┘
   │         │         │         │         │         │         │
┌──▼──┐ ┌───▼──┐ ┌────▼──┐ ┌───▼──┐ ┌───▼──┐ ┌────▼──┐ ┌────▼──────────┐
│文档  │ │要素  │ │合规   │ │背书  │ │风险  │ │报告  │ │流转状态查询  │
│解析  │ │抽取  │ │检索   │ │链路  │ │评估  │ │生成  │ │    Agent     │
│Agent │ │Agent │ │Agent  │ │Agent │ │Agent │ │Agent │ │(新增 v1.1)   │
└──┬──┘ └───┬──┘ └────┬──┘ └───┬──┘ └───┬──┘ └────┬──┘ └──────┬────────┘
   │         │         │         │         │         │           │
┌──▼─────────▼─────────▼─────────▼─────────▼─────────▼───────────▼────────┐
│                            基础设施层                                     │
│  Milvus(向量库) · Redis(缓存/队列) · MySQL(结构化存储)                    │
│  Qwen2.5-14B(推理) · OCR引擎 · PDF解析服务                               │
│  票交所报文网关适配层 · 前置机状态接口 · 行内核心系统查询接口             │
└───────────────────────────────────────────────────────────────────────────┘
```

### 2.2 Agent 通信机制

- **同步调用**：关键路径上的 Agent 链（解析 → 抽取 → 合规检索）采用顺序调用，保证数据依赖正确性
- **异步并行**：合规检索、背书核查、风险评估三个 Agent 在要素抽取完成后并行触发，通过 Celery + Redis 调度
- **消息总线**：Agent 间通过结构化 JSON 消息传递上下文，消息格式遵循统一 Schema（见第六节）
- **人工介入节点**：风险等级为"高危"时自动暂停流程，推送人工复核队列

---

## 三、各 Agent 详细需求

### 3.1 主控编排 Agent（Orchestrator Agent）

**职责定位**：全局任务生命周期管理，不执行业务逻辑，只做调度与聚合。

#### 3.1.1 核心能力

| 能力项 | 说明 |
|--------|------|
| 任务拆解 | 根据票据类型（汇票/本票/支票）和提交材料清单，动态规划 Agent 执行图（DAG） |
| 上下文传递 | 维护全局 Context Store，各 Agent 结果写入后可供后续 Agent 读取 |
| 异常重试 | Agent 超时或失败时，按退避策略（指数退避，最多 3 次）重试 |
| 进度回调 | 实时更新任务状态（PENDING / RUNNING / PARTIAL_DONE / DONE / FAILED） |
| 优先级调度 | 支持紧急票据插队处理，VIP 客户通道独立资源池 |

#### 3.1.2 任务状态机

```
SUBMITTED → PARSING → EXTRACTING → [COMPLIANCE_CHECK ‖ ENDORSEMENT_CHECK ‖ RISK_ASSESS]
         → REPORT_GENERATING → COMPLETED
         ↕ (任意阶段可跳转)
         FAILED / HUMAN_REVIEW_REQUIRED
```

#### 3.1.3 接口规范

```
POST /api/v1/tasks/submit          # 提交审核任务
GET  /api/v1/tasks/{task_id}       # 查询任务状态
POST /api/v1/tasks/{task_id}/retry # 手动触发重试
GET  /api/v1/tasks/{task_id}/result # 获取审核结果
```

---

### 3.2 文档解析 Agent（Document Parser Agent）

**职责定位**：将原始票据影像/PDF 转化为结构化文本，为后续 Agent 提供干净的输入数据。

#### 3.2.1 输入支持

- 票据正反面影像（JPG/PNG，分辨率要求 ≥ 300 DPI）
- 合同/协议 PDF（支持扫描件与原生 PDF 双模式）
- 背书链凭证图片（可为多张，按顺序编号）

#### 3.2.2 处理流程

```
原始文件 → 文件类型识别 → 图像预处理（去噪/旋转矫正/二值化）
        → OCR 引擎调用（PaddleOCR / Tesseract）
        → 版面分析（正面/背面/附件区域切分）
        → 文本清洗（错字纠正、格式标准化）
        → 输出结构化文本 + 置信度评分
```

#### 3.2.3 输出 Schema

```json
{
  "doc_id": "DOC_20240115_001",
  "doc_type": "COMMERCIAL_DRAFT",  // 商业汇票
  "pages": [
    {
      "page_no": 1,
      "side": "FRONT",
      "ocr_text": "...",
      "confidence": 0.97,
      "regions": {
        "header": "...",
        "body": "...",
        "seal_area": "...",
        "endorsement_area": "..."
      }
    }
  ],
  "parse_quality": "HIGH",  // HIGH / MEDIUM / LOW
  "warnings": ["第2页图像模糊，置信度低于0.85"]
}
```

#### 3.2.4 异常处理

- 置信度 < 0.75 的区域标记为 `LOW_CONFIDENCE`，触发人工复核
- 完全无法识别时返回 `PARSE_FAILED`，携带失败原因，任务流转至人工队列

---

### 3.3 要素抽取 Agent（Element Extraction Agent）

**职责定位**：从 OCR 文本中精准抽取票据各关键要素，构建票据结构化数据模型。

#### 3.3.1 抽取要素清单

**汇票要素（共 18 项）**

| 要素编号 | 要素名称 | 是否必填 | 校验规则 |
|----------|----------|----------|----------|
| E001 | 票据类型 | 必填 | 枚举：商业承兑汇票/银行承兑汇票 |
| E002 | 出票日期 | 必填 | 格式：YYYY年MM月DD日，不得早于1990年 |
| E003 | 到期日 | 必填 | 须晚于出票日，最长期限 1 年 |
| E004 | 票面金额（大写） | 必填 | 须与小写金额一致 |
| E005 | 票面金额（小写） | 必填 | 数字格式校验 |
| E006 | 出票人名称 | 必填 | 与工商信息比对 |
| E007 | 出票人账号 | 必填 | 银行账号格式校验 |
| E008 | 出票人开户行 | 必填 | |
| E009 | 收款人名称 | 必填 | |
| E010 | 承兑人名称 | 条件必填 | 银票必填 |
| E011 | 承兑人账号 | 条件必填 | 银票必填 |
| E012 | 付款地 | 必填 | |
| E013 | 出票地 | 必填 | |
| E014 | 出票人印章 | 必填 | 印章识别标志位 |
| E015 | 承兑日期 | 条件必填 | 银票必填 |
| E016 | 承兑人签章 | 条件必填 | 银票必填 |
| E017 | 汇票号码 | 必填 | 格式正则校验 |
| E018 | 是否可转让 | 必填 | 枚举：可转让/不可转让 |

**合同要素（共 12 项）**

| 要素编号 | 要素名称 | 说明 |
|----------|----------|------|
| C001 | 合同编号 | |
| C002 | 签订日期 | |
| C003 | 甲方信息 | 名称、法人、联系方式 |
| C004 | 乙方信息 | 名称、法人、联系方式 |
| C005 | 标的金额 | 须与票据金额匹配 |
| C006 | 付款条款 | 付款方式、期限 |
| C007 | 票据条款 | 涉及票据使用的约定 |
| C008 | 违约条款 | |
| C009 | 争议解决 | |
| C010 | 合同有效期 | |
| C011 | 双方签章 | |
| C012 | 见证方信息 | 如有 |

#### 3.3.2 抽取策略

```
结构化 Prompt 设计（基于 Qwen2.5-14B）：

System: 你是票据要素抽取专家，严格按照中国票据法规定的要素项进行结构化提取。
        对无法确定的要素，填写 null 并在 uncertain_reasons 中说明。
        不得推断或补全原文中未出现的内容。

User:   [OCR文本] + [要素抽取JSON Schema]
```

#### 3.3.3 金额一致性校验

```python
# 大小写金额比对逻辑（示例）
rules:
  - 大写金额转换为数字后与小写金额误差 ≤ 0.01 元
  - 合同标的金额与票面金额偏差 ≤ 5%（超出触发预警）
  - 多张票据合计金额须与合同总额一致
```

---

### 3.4 合规检索 Agent（Compliance Retrieval Agent）

**职责定位**：基于 RAG 从法规知识库中检索与当前票据相关的合规要求，并逐项进行合规性判断。

#### 3.4.1 知识库结构

```
知识库分类：
├── 法律法规层
│   ├── 《票据法》（2004修正版）
│   ├── 《支付结算办法》（人行）
│   └── 《电子商业汇票业务管理办法》
├── 监管规则层
│   ├── 上交所票据市场交易规则
│   ├── 银行间市场票据交易规范
│   └── ECDS 系统操作规程
├── 行内合规层
│   ├── 票据业务操作手册
│   ├── 贴现业务审核指引
│   └── 风险管控内部规定
└── 案例库
    ├── 历史违规案例（脱敏）
    └── 监管处罚公告摘录
```

#### 3.4.2 分块策略

- **条款级分块**：以"第X条"为最小分块单位，保留完整条款，不跨条截断
- **层级编号保留**：分块元数据携带 `{law_name, chapter, article, clause}` 四级索引
- **交叉引用处理**："参见第X条"类引用自动展开，将被引条款并入上下文窗口

#### 3.4.3 检索与判断流程

```
输入：结构化票据要素 + 票据类型标签
  ↓
Query 构建：针对每个要素生成检索问题
  （例："商业承兑汇票的出票日期有何法定要求？"）
  ↓
向量检索：Top-K=5，相似度阈值 ≥ 0.75
  ↓
重排序：BM25 + 语义相似度混合排序
  ↓
合规判断 Prompt：逐要素对照法规条文进行 PASS/FAIL/WARNING 判定
  ↓
输出：合规检查报告（含条款引用）
```

#### 3.4.4 合规检查输出格式

```json
{
  "compliance_results": [
    {
      "element": "E003",
      "element_name": "到期日",
      "value": "2024年06月30日",
      "status": "PASS",
      "legal_basis": [
        {
          "law": "《票据法》",
          "article": "第十条",
          "clause": "第二款",
          "content": "汇票的到期日应当确定，不得附有条件..."
        }
      ],
      "detail": "到期日格式合规，距出票日165天，未超过一年最长期限"
    },
    {
      "element": "E014",
      "element_name": "出票人印章",
      "status": "FAIL",
      "legal_basis": [...],
      "detail": "出票人印章模糊，无法确认与营业执照名称一致性"
    }
  ],
  "overall_status": "FAIL",
  "fail_count": 1,
  "warning_count": 2,
  "pass_count": 15
}
```

---

### 3.5 背书链路核查 Agent（Endorsement Chain Agent）

**职责定位**：还原完整的票据背书转让链路，识别背书断链、不合规背书、禁止背书等问题。

#### 3.5.1 背书链路重建

```
背书链重建算法：
1. 从 OCR 结果中提取所有背书区域文本
2. 识别每次背书的：背书人、被背书人、背书日期、背书签章
3. 按日期排序，构建有向图 G(V, E)
   - 节点 V：每个票据持有方
   - 边 E：背书转让记录
4. 验证图的连通性：
   - 收款人 → 背书人1 → 背书人2 → ... → 当前持有人
   - 任意断点标记为 CHAIN_BROKEN
```

#### 3.5.2 背书合规性校验项

| 校验项 | 规则说明 | 违规严重度 |
|--------|----------|------------|
| 背书连续性 | 前手被背书人 = 后手背书人名称一致 | 致命 |
| 禁止背书识别 | "不得转让"字样后的背书无效 | 致命 |
| 背书日期合理性 | 背书日期须在出票日之后、到期日之前 | 严重 |
| 背书签章完整性 | 每笔背书必须有背书人签章 | 严重 |
| 部分背书检测 | 不允许部分金额背书 | 严重 |
| 质押背书识别 | 质押背书需标注"质押"字样并核查质押登记 | 中等 |
| 委托收款背书 | 委托收款不能再次背书转让 | 中等 |
| 涂改痕迹检测 | 背书区域涂改须有更正签章 | 中等 |
| 空白背书检测 | 中国票据法不支持空白背书 | 严重 |

#### 3.5.3 关联方识别

- 背书链中若出现**关联企业**（同一实控人、关联交易），自动标记并提示风控
- 接入工商数据接口，实时查询背书链各方的企业状态（注销/吊销/正常）
- 识别**虚假贸易背景**风险信号：背书链过长（超过 5 手）、关联方循环背书

#### 3.5.4 输出示例

```json
{
  "endorsement_chain": [
    {"seq": 1, "endorser": "A公司", "endorsee": "B公司", "date": "2024-01-10", "valid": true},
    {"seq": 2, "endorser": "B公司", "endorsee": "C公司", "date": "2024-01-18", "valid": true},
    {"seq": 3, "endorser": "D公司", "endorsee": "E公司", "date": "2024-01-25", "valid": false,
     "issues": ["背书人D公司与前手被背书人C公司名称不一致，背书链断裂"]}
  ],
  "chain_status": "BROKEN",
  "total_endorsements": 3,
  "valid_endorsements": 2,
  "risk_flags": ["背书链断裂(第3笔)", "B公司企业状态异常(工商已注销)"]
}
```

---

### 3.6 合同审核 Agent（Contract Review Agent）

**职责定位**：审核贸易合同与票据的匹配性，识别合同条款风险，验证贸易背景真实性。

#### 3.6.1 票据-合同匹配验证

```
匹配项清单：
□ 合同金额 vs 票据金额（允差 ≤ 5%）
□ 合同付款期限 vs 票据到期日（需覆盖）
□ 买方（付款方）vs 汇票出票人/承兑人
□ 卖方（收款方）vs 汇票收款人
□ 合同签订日期须早于出票日期
□ 合同约定付款方式需包含票据支付条款
```

#### 3.6.2 合同条款风险识别

| 风险类型 | 识别关键词/逻辑 | 风险等级 |
|----------|----------------|----------|
| 阴阳合同风险 | 合同金额与附件/补充协议不一致 | 高危 |
| 无真实贸易背景 | 合同标的物描述模糊、无具体规格/数量 | 高危 |
| 循环贸易 | 甲乙双方在其他合同中角色对调 | 高危 |
| 合同要素缺失 | 缺少标的物、价款、履行期限三要素之一 | 严重 |
| 付款条款歧义 | 付款方式表述模糊，存在多种解释可能 | 中等 |
| 违约条款不对等 | 对一方明显不利的违约安排 | 低 |
| 管辖条款风险 | 约定境外仲裁/争议解决 | 中等 |
| 印章与签字缺失 | 合同页未见双方完整签章 | 严重 |

#### 3.6.3 贸易背景真实性评分模型

```
评分维度（满分100分）：

1. 合同要素完整性        (0-25分)
   - 必填要素完整度 × 25

2. 金额合理性           (0-25分)  
   - 合同金额与行业均价对比
   - 历史交易金额一致性

3. 关联方合规性         (0-25分)
   - 交易方资质合规
   - 无关联方循环交易

4. 合同条款合规性       (0-25分)
   - 无高危条款
   - 付款条款清晰

评分区间：
≥ 80分：低风险，直接通过
60-79分：中风险，人工抽查
< 60分：高风险，强制人工审核
```

---

### 3.7 风险评估 Agent（Risk Assessment Agent）

**职责定位**：汇聚所有 Agent 输出，进行综合风险评分，输出最终风险结论与处置建议。

#### 3.7.1 风险评估维度

```
风险评估框架（四维模型）：

┌──────────────────┬──────────────────────────────────────┐
│ 维度             │ 子指标                               │
├──────────────────┼──────────────────────────────────────┤
│ 1. 票据本体风险  │ 要素完整性、印章真实性、金额合规性   │
│ 2. 合规风险      │ 法规符合度、政策合规性               │
│ 3. 背书链路风险  │ 链路完整性、关联方风险、企业状态     │
│ 4. 贸易背景风险  │ 合同匹配度、背景真实性、交易合理性   │
└──────────────────┴──────────────────────────────────────┘
```

#### 3.7.2 风险评级标准

| 风险等级 | 描述 | 处置动作 |
|----------|------|----------|
| **低风险** (Green) | 所有检查项通过，无预警 | 系统自动审批通过 |
| **中低风险** (Yellow-Low) | 存在 Warning 项，无 Fail | 系统通过 + 标注关注项 |
| **中高风险** (Yellow-High) | 存在 1-2 个非致命 Fail | 发送业务主管审核 |
| **高风险** (Orange) | 存在致命 Fail 或多个 Fail | 发送风控部门审核 |
| **极高风险** (Red) | 背书链断裂、疑似欺诈信号 | 冻结任务 + 合规部介入 |

#### 3.7.3 风险归因说明

```
每项风险结论须包含：
- 风险描述（What）：具体问题是什么
- 法规依据（Why）：对应哪条法规/内部规定
- 影响范围（Impact）：对本次交易的影响程度
- 处置建议（How）：建议如何修正或规避
```

---

### 3.8 报告生成 Agent（Report Generation Agent）

**职责定位**：将所有 Agent 输出整合为规范的结构化审核报告，支持多格式输出。

#### 3.8.1 报告结构

```
审核报告目录：

1. 报告摘要（Executive Summary）
   - 任务ID、提交时间、完成时间、耗时
   - 整体风险等级（彩色标识）
   - 通过/警告/失败项目数量统计
   - 处置建议一句话总结

2. 票据基本信息
   - 票据类型、票面金额、期限
   - 出票人/收款人/承兑人信息

3. 要素完整性检查结果（18项逐一列示）

4. 合规性检查结果
   - 逐条法规对照结果
   - 条款引用（法规名称 + 条款号 + 原文）

5. 背书链路分析
   - 背书链路可视化（文字版有向图）
   - 各节点企业状态

6. 合同审核结果
   - 票据-合同匹配性
   - 贸易背景评分

7. 风险评估总结
   - 四维风险雷达图数据
   - 各维度风险详情
   - 综合风险评级

8. 处置建议清单
   - 按优先级排序的待办事项

9. 审核日志
   - 各 Agent 执行时间戳
   - 异常记录
```

#### 3.8.2 输出格式

- **JSON**：供系统集成、数据分析使用
- **PDF**：带公司抬头、电子签章的正式报告，供存档、客户交付
- **API 回调**：审核完成后 Webhook 推送至业务系统

---

### 3.9 票据流转状态查询 Agent（Bill Flow Tracking Agent）⭐ 新增

**职责定位**：实时追踪票据业务报文在「发起行 → 前置机 → 票交所 → 对手行」各节点的流转状态，识别当前卡点位置、超时节点及异常原因，为业务人员提供可解释的全链路进度视图。

#### 3.9.1 业务背景与痛点

票据业务（提示承兑、贴现、背书转让、质押等）在处理过程中，报文须经过多个系统节点的往返传递，每一跳均有「发送报文」与「回执确认」两个消息。现有问题：

| 痛点 | 描述 |
|------|------|
| 状态不透明 | 业务员只知道"还在处理中"，不知道卡在哪个系统节点 |
| 超时难定位 | 票交所截止时间临近时，无法快速判断是己方前置机延迟还是对手行未回应 |
| 多系统查询繁琐 | 需要分别登录核心系统、前置机监控、票交所查询平台三套系统人工比对 |
| 异常信息晦涩 | 报文错误码（如 `E0034`）无法直接转化为可操作的业务提示 |

#### 3.9.2 覆盖的业务类型与标准流程

**（A）提示承兑**

```
┌────────────────────────────────────────────────────────────────────────┐
│  提示承兑报文流转全链路（每步含"发送"和"回执"两个消息节点）           │
└────────────────────────────────────────────────────────────────────────┘

STEP 1  ┌─────────────┐  承兑申请报文(0201)   ┌──────────────┐
        │  发起行      │ ─────────────────────▶│  行内前置机  │
        │  业务系统    │ ◀─────────────────────│              │
        └─────────────┘  接收确认回执(ACK0201) └──────┬───────┘
                                                       │
STEP 2                                                 │  报文组装&路由
                                                       ▼
                                               ┌──────────────┐  标准化报文   ┌──────────────┐
                                               │  行内前置机  │ ────────────▶│   票 交 所   │
                                               │              │ ◀────────────│   (ECDS)     │
                                               └──────────────┘  签收确认     └──────┬───────┘
                                                                                      │
STEP 3                                                                                │  路由至承兑行
                                                                                      ▼
                                                                             ┌──────────────────┐
                                                                             │  承兑行前置机     │
                                               ┌──────────────┐  承兑请求   │                  │
                                               │   票 交 所   │ ──────────▶│                  │
                                               │              │ ◀──────────│                  │
                                               └──────────────┘  接收确认  └────────┬─────────┘
                                                                                     │
STEP 4                                                                               │  内部审批/处理
                                                                                     ▼
                                                                          ┌──────────────────────┐
                                                                          │  承兑行业务系统       │
                                                                          │  （人工审批/自动审批）│
                                                                          └──────────┬───────────┘
                                                                                     │
STEP 5                                                                               │  承兑结果应答
                                                                                     ▼
                                               ┌──────────────┐  应答报文   ┌──────────────────┐
                                               │   票 交 所   │ ◀─────────│  承兑行前置机     │
                                               │              │ ──────────▶│                  │
                                               └──────┬───────┘  回执确认  └──────────────────┘
                                                      │
STEP 6                                                │  转发应答至发起行
                                                      ▼
                                       ┌──────────────────┐  应答转发   ┌─────────────┐
                                       │  行内前置机       │ ◀─────────│  票 交 所   │
                                       │                  │ ──────────▶│             │
                                       └────────┬─────────┘  回执确认  └─────────────┘
                                                │
STEP 7                                          │  结果入账通知
                                                ▼
                                       ┌─────────────────┐
                                       │  发起行业务系统  │
                                       │  （承兑结果入账）│
                                       └─────────────────┘
```

**（B）其他业务类型标准步骤数**

| 业务类型 | 报文类型代码 | 标准节点数 | 最大超时时限 |
|----------|-------------|-----------|-------------|
| 提示承兑 | 0201/0202 | 7 步 / 14 个消息节点 | 24小时（工作日） |
| 提示付款 | 0301/0302 | 7 步 / 14 个消息节点 | 当日营业时间内 |
| 贴现申请 | 0401/0402 | 5 步 / 10 个消息节点 | 4小时 |
| 背书转让 | 0101/0102 | 5 步 / 10 个消息节点 | 2小时 |
| 质押背书 | 0501/0502 | 6 步 / 12 个消息节点 | 2小时 |
| 撤销申请 | 0901/0902 | 4 步 / 8 个消息节点 | 1小时 |
| 拒绝承兑 | 0203/0204 | 3 步 / 6 个消息节点 | 30分钟 |

#### 3.9.3 Agent 核心能力

| 能力项 | 说明 |
|--------|------|
| 多维度查询入口 | 支持按「票据号码 / 业务流水号 / 发起时间范围 / 业务类型」组合查询 |
| 全节点状态拉取 | 并发调用发起行前置机、票交所查询接口、对手行查询接口（若开放），聚合各节点最新状态 |
| 当前卡点识别 | 精确定位「最后成功节点」与「等待中节点」，输出人类可读的卡点描述 |
| 超时预警 | 对比业务规定时限，计算剩余时间，分级预警（黄色≤30%时限剩余，红色≤10%） |
| 异常码翻译 | 将前置机/票交所的原始错误码（如 `ECDS_E0034`）映射为业务语言 |
| 历史轨迹回放 | 展示该笔业务从发起至今所有消息节点的完整时间轴 |
| 主动推送 | 状态发生变化时通过 WebSocket / 企微 / 短信主动通知业务员 |

#### 3.9.4 查询流程设计

```
用户输入（票据号/流水号）
        │
        ▼
┌───────────────────────┐
│  1. 意图识别 & 参数解析 │  ← LLM解析自然语言查询
│  "帮我查一下这笔承兑   │     提取: 票据号、业务类型、时间范围
│   现在到哪一步了"      │
└──────────┬────────────┘
           │
           ▼
┌───────────────────────┐
│  2. 业务流水号关联     │  ← 查询 MySQL: 票据号→业务流水号
│     & 业务类型确认     │     确认是哪种业务（承兑/付款/贴现...）
└──────────┬────────────┘
           │
     ┌─────┴──────────────────────────────────┐
     │ 3. 并发查询各节点状态（异步并行）        │
     ├───────────────┬──────────────┬──────────┤
     ▼               ▼              ▼          ▼
┌─────────┐   ┌──────────┐  ┌──────────┐  ┌──────────┐
│行内核心  │   │行内前置机 │  │票交所    │  │对手行    │
│系统查询  │   │日志查询  │  │查询接口  │  │状态接口  │
│(本行DB) │   │(REST API)│  │(ECDS API)│  │(若开放)  │
└────┬────┘   └────┬─────┘  └────┬─────┘  └────┬─────┘
     └─────────────┴──────────────┴──────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │  4. 状态聚合 & 链路拼接        │
              │  将各节点返回数据按时间戳排序  │
              │  重建完整消息时间轴            │
              └──────────────┬────────────────┘
                             │
                             ▼
              ┌───────────────────────────────┐
              │  5. 卡点识别 & 超时计算        │
              │  定位最后成功节点              │
              │  计算等待时长 vs 规定时限      │
              └──────────────┬────────────────┘
                             │
                             ▼
              ┌───────────────────────────────┐
              │  6. LLM 生成自然语言解释       │
              │  错误码 → 业务语言翻译         │
              │  输出结构化 + 自然语言双格式   │
              └──────────────────────────────┘
```

#### 3.9.5 消息节点状态模型

每个报文节点记录以下状态属性：

```json
{
  "node_id": "STEP_3_SEND",
  "step_no": 3,
  "direction": "SEND",
  "from_system": "ECDS",
  "to_system": "ACCEPTOR_FRONT_PROCESSOR",
  "msg_type": "0201",
  "msg_seq": "2024011500000123",
  "status": "PENDING",
  "send_time": "2024-01-15T09:32:10.456Z",
  "ack_time": null,
  "elapsed_ms": 87340,
  "timeout_threshold_ms": 300000,
  "timeout_ratio": 0.29,
  "error_code": null,
  "error_msg": null
}
```

**节点 status 枚举值：**

| Status | 含义 | 展示颜色 |
|--------|------|----------|
| `SENT` | 已发出，等待回执 | 蓝色闪烁 |
| `ACK_RECEIVED` | 对方已签收 | 蓝色实心 |
| `PROCESSING` | 对方系统处理中 | 黄色 |
| `RESPONDED` | 对方已回应结果 | 绿色（成功）/ 红色（拒绝） |
| `TIMEOUT` | 超过规定时限未收到回执 | 红色告警 |
| `ERROR` | 报文异常/格式错误 | 红色 |
| `CANCELLED` | 已撤销 | 灰色 |

#### 3.9.6 全链路时间轴输出格式

```json
{
  "query_id": "QRY_20240115_001",
  "bill_no": "310000000012345678",
  "business_type": "ACCEPTANCE_PRESENTATION",
  "business_type_name": "提示承兑",
  "initiator_bank": "某商业银行股份有限公司",
  "acceptor_bank": "中国工商银行",
  "submitted_at": "2024-01-15T09:30:00.000Z",
  "current_status": "PENDING_ACCEPTOR_RESPONSE",
  "current_status_name": "等待承兑行应答",
  "stuck_node": {
    "step_no": 4,
    "description": "承兑行业务系统内部审批中",
    "waiting_since": "2024-01-15T09:35:22.000Z",
    "elapsed_minutes": 87,
    "timeout_minutes": 1440,
    "timeout_ratio": 0.06,
    "warning_level": "NORMAL"
  },
  "timeline": [
    {
      "step_no": 1,
      "step_name": "发起行提交申请",
      "events": [
        {"event": "SEND",        "time": "09:30:00", "system": "发起行业务系统 → 前置机", "status": "ACK_RECEIVED", "elapsed_ms": 230},
        {"event": "ACK",         "time": "09:30:00", "system": "前置机 → 发起行业务系统", "status": "ACK_RECEIVED", "elapsed_ms": 230}
      ]
    },
    {
      "step_no": 2,
      "step_name": "前置机上报票交所",
      "events": [
        {"event": "SEND",        "time": "09:30:01", "system": "前置机 → 票交所(ECDS)", "status": "ACK_RECEIVED", "elapsed_ms": 1120},
        {"event": "ACK",         "time": "09:30:02", "system": "票交所(ECDS) → 前置机", "status": "ACK_RECEIVED", "elapsed_ms": 1120}
      ]
    },
    {
      "step_no": 3,
      "step_name": "票交所转发至承兑行",
      "events": [
        {"event": "SEND",        "time": "09:30:03", "system": "票交所(ECDS) → 承兑行前置机", "status": "ACK_RECEIVED", "elapsed_ms": 890},
        {"event": "ACK",         "time": "09:30:04", "system": "承兑行前置机 → 票交所(ECDS)", "status": "ACK_RECEIVED", "elapsed_ms": 890}
      ]
    },
    {
      "step_no": 4,
      "step_name": "承兑行内部审批",
      "events": [
        {"event": "PROCESSING",  "time": "09:35:22", "system": "承兑行业务系统", "status": "PROCESSING", "elapsed_ms": 5238000}
      ]
    },
    {"step_no": 5, "step_name": "承兑行回应票交所",     "events": [{"status": "NOT_STARTED"}]},
    {"step_no": 6, "step_name": "票交所转发结果至发起行","events": [{"status": "NOT_STARTED"}]},
    {"step_no": 7, "step_name": "发起行结果入账",        "events": [{"status": "NOT_STARTED"}]}
  ],
  "natural_language_summary": "该笔提示承兑申请已于09:30成功提交，报文已顺利流转至工商银行承兑行前置机并签收确认。当前正在等待工商银行内部审批处理，已等待约87分钟，距24小时处理时限尚有约23小时，目前状态正常。如需加急处理，可通过行际联系渠道催促对手行审批。",
  "suggested_actions": [
    "当前状态正常，无需干预，继续等待承兑行审批结果",
    "如距截止时间不足2小时仍未收到回应，建议拨打工商银行票据业务热线: 95588"
  ]
}
```

#### 3.9.7 超时预警与自动升级机制

```
预警等级定义：

NORMAL   (绿色)  elapsed / timeout_threshold < 50%   → 正常监控
WATCH    (蓝色)  50% ≤ ratio < 70%                   → 系统记录，不通知
WARNING  (黄色)  70% ≤ ratio < 90%                   → 推送给经办人
URGENT   (橙色)  90% ≤ ratio < 100%                  → 推送给经办人+部门主管
OVERDUE  (红色)  ratio ≥ 100%                        → 推送全链路 + 开启SLA违规流程

自动升级动作：
  WARNING → 企微/短信通知经办人，附快捷催报链接
  URGENT  → 通知经办人+主管，系统自动记录SLA预警工单
  OVERDUE → 通知运营管理层，自动生成SLA违规事件单，触发应急预案
```

#### 3.9.8 异常码翻译库（部分示例）

| 原始错误码 | 来源系统 | 业务含义 | 建议处置 |
|-----------|----------|----------|----------|
| `ECDS_E0034` | 票交所 | 票据已被其他机构提示，存在重复提示 | 立即撤销本次申请，核查票据实际持有状态 |
| `ECDS_E0087` | 票交所 | 票据不在有效期内（已过期或未到承兑期） | 核查票据到期日，确认是否超过付款提示期 |
| `FRONT_TIMEOUT` | 行内前置机 | 前置机报文发送超时，可能为网络问题 | 检查前置机网络连接状态，联系系统管理员 |
| `ACC_REJECT_001` | 承兑行 | 承兑行拒绝承兑 — 贸易背景不符 | 与承兑行商务协调，补充贸易背景证明材料 |
| `ACC_REJECT_002` | 承兑行 | 承兑行拒绝承兑 — 出票人账户状态异常 | 联系出票人核查开户行账户状态 |
| `ACC_REJECT_003` | 承兑行 | 承兑行拒绝承兑 — 授信额度不足 | 与出票人确认授信情况，必要时寻求其他融资安排 |
| `ECDS_SYS_MAINT` | 票交所 | 票交所系统维护中（节假日/深夜窗口） | 等待系统维护窗口结束，通常为2小时内，勿重复发送 |
| `SIGN_VERIFY_FAIL` | 前置机 | 数字签名验证失败，报文可能被篡改 | 立即停止该笔业务，上报信息安全事件 |

#### 3.9.9 自然语言交互接口

Agent 支持通过自然语言对话方式查询，LLM 负责意图识别与参数提取：

```
用户输入示例 → Agent 解析与处理

"帮我查一下310000000012345678这张票的承兑进度"
  → 提取: bill_no=310000000012345678, business_type=ACCEPTANCE_PRESENTATION
  → 返回: 全链路时间轴 + 自然语言摘要

"昨天下午提交的那笔贴现还没到账，查一下"
  → 提取: 时间范围=yesterday_afternoon, business_type=DISCOUNT, status_filter=PENDING
  → 返回: 符合条件的业务列表 + 各笔当前卡点

"最近一小时有没有超时的票据业务"
  → 提取: 时间范围=last_1_hour, warning_level≥OVERDUE
  → 返回: 超时业务清单 + 每笔的超时时长和建议动作

"3108开头的那张票交所那边收到了吗"
  → 提取: bill_no_prefix=3108, 查询节点=ECDS_ACK
  → 返回: 票交所签收节点的具体状态和时间戳
```

#### 3.9.10 与其他 Agent 的协作关系

```
流转状态查询 Agent 触发其他 Agent 的场景：

OVERDUE 事件
  └─▶ 风险评估 Agent：将超时事件纳入该笔票据风险评分，影响后续贴现审批

ERROR(SIGN_VERIFY_FAIL)
  └─▶ 合规检索 Agent：检索"报文安全事件"相关合规处置流程

ACC_REJECT_001 (贸易背景不符)
  └─▶ 合同审核 Agent：重新审核关联合同，输出贸易背景补充材料清单

背书链断裂警告
  └─▶ 背书链路 Agent：触发重新核查背书链完整性
```

#### 3.9.11 接口规范

```
# 精确查询
GET  /api/v1/flow/status?bill_no={bill_no}&business_type={type}
GET  /api/v1/flow/status?serial_no={serial_no}

# 批量查询（运营监控）
POST /api/v1/flow/batch-status
Body: { "bill_nos": [...], "time_range": {...}, "warning_level": "WARNING" }

# 自然语言查询
POST /api/v1/flow/query
Body: { "natural_language": "帮我查一下这笔承兑到哪一步了", "context": {...} }

# WebSocket 实时推送订阅
WS   /ws/v1/flow/subscribe?bill_no={bill_no}

# 历史轨迹回放
GET  /api/v1/flow/history?bill_no={bill_no}&from={ts}&to={ts}
```

---

### 3.10 欺诈检测 Agent（Fraud Detection Agent）⭐ 新增

**职责定位**：对疑似伪造、篡改或存在欺诈信号的票据进行深度专项分析，独立于常规合规审核流程运行，可由风控系统信号、人工举报或随机抽检三种方式触发。

#### 3.10.1 触发机制

| 触发方式 | 触发条件 | 优先级 |
|----------|----------|--------|
| 风控系统自动触发 | 异常交易模式（短时间大量同类票据 / 关联方集中提交） | 高 |
| 业务员人工举报 | 业务员在审核中发现可疑线索 | 高 |
| 随机抽检 | 按设定比例（默认 5%）对正常通过的票据随机深查 | 中 |
| 定期专项检查 | 每周对高金额票据（> 500 万）做全量欺诈复查 | 低 |

#### 3.10.2 五维检测能力（四路并行）

**维度一：印章真伪识别**

```
方法：
  1. 提取票据印章区域图像（OCR 标注的印章坐标）
  2. 与历史印章特征库进行向量相似度比对
  3. 分析印章颜色分布、边缘锐度、压痕特征
  
判断逻辑：
  相似度 ≥ 0.92  → 真实印章（PASS）
  0.75 ≤ 相似度 < 0.92 → 疑似问题（WARNING，人工比对）
  相似度 < 0.75  → 疑似伪造（FAIL，冻结）
  
特殊处理：
  首次出现的出票人 → 无历史印章，标记为 UNKNOWN，要求提供工商印章备案件
```

**维度二：重复票据查询**

```
查询逻辑：
  1. 按票据号码精确查询全库（MySQL + Redis 缓存）
  2. 同一票据号码出现在多笔提交中 → 双重贴现/承兑风险
  3. 模糊查询：号码相差一位 → 疑似号码篡改（人工复核）

防重复提交：
  同一业务流水号 5 分钟内重复提交 → 幂等处理，返回原结果
  同一票据号码跨业务类型提交 → 告警（如既贴现又质押）
```

**维度三：图像篡改检测**

```
检测项：
  1. 文字区域墨迹分析：涂改、覆盖痕迹识别
  2. 笔迹压痕分析：正面修改在背面是否留有压痕
  3. 图像元数据检测：EXIF 信息异常（拍摄设备、时间戳不合理）
  4. 打印字体一致性：同一票据中多种字体混用（疑似拼接）
  5. 金额区域特殊检测：数字区域放大分析，识别叠写/插入痕迹

模型：基于 CNN 的图像异常检测模型（ResNet50 fine-tuned on bill dataset）
```

**维度四：关联网络分析**

```
构建要素：
  1. 从背书链、合同数据中提取全部参与方实体
  2. 查询工商数据库，补充股权穿透关系
  3. 构建关联图谱 G(实体, 关系)

识别模式：
  循环背书：A→B→C→A 的闭环背书链
  壳公司特征：注册资本 < 100万 + 成立 < 6个月 + 无实际经营地址
  关联方集中：同一控制人旗下多个主体互相背书
  虚假贸易网络：买卖双方在其他合同中角色互换（AB互为买卖方）
```

**维度五：历史案例比对**

```
比对库：
  - 行内历史违规案例（脱敏处理）
  - 监管处罚公告摘录（人行、银保监会）
  - 黑名单主体库（出票人/承兑人/持票人）

比对维度：
  主体名称黑名单   → 精确匹配 + 近似名称匹配
  行为模式相似度  → 向量化案例特征，Top-K 相似案例检索
  监管处罚记录    → 5年内有票据类处罚记录的主体告警
```

#### 3.10.3 欺诈评分模型

```
综合欺诈风险分值 = 加权求和（0.0 - 1.0）：

  印章真伪        权重 0.30
  重复票据        权重 0.25（发现重复直接置 1.0，覆盖其他维度）
  图像篡改        权重 0.25
  关联网络异常    权重 0.15
  历史黑名单      权重 0.05

判定区间：
  0.0 - 0.30  → 低风险，转常规合规流程，附欺诈检测报告
  0.30 - 0.70 → 中等风险，风控部门重点复核，要求补充材料
  0.70 - 1.0  → 高欺诈风险，冻结业务，合规部介入，必要时上报监管
```

#### 3.10.4 应急响应动作

```
高欺诈风险（分值 > 0.70）触发的自动动作：
  1. 业务立即冻结，阻止任何后续操作
  2. 通知合规部（短信 + 系统工单）
  3. 通知风控部（邮件 + 企微）
  4. 生成欺诈检测详细报告（各维度证据截图 + 评分明细）
  5. 若涉及已入账业务，触发资金冻结流程（需人工确认后执行）
  6. 保全证据：原始图像 + OCR 结果 + 所有中间处理结果归档不可篡改存储
```

#### 3.10.5 与其他 Agent 的联动

```
欺诈检测 Agent → 合规检索 Agent
  高欺诈风险票据触发深度合规检索，检索"票据欺诈处置"相关法规条款

欺诈检测 Agent → 背书链路 Agent  
  关联网络异常信号触发背书链路 Agent 进行更深度的穿透核查

欺诈检测 Agent → 报告生成 Agent
  欺诈检测结论并入最终审核报告，作为独立章节输出
```

#### 3.10.6 接口规范

```
# 主动触发欺诈检测
POST /api/v1/fraud/detect
Body: { "bill_no": "...", "trigger_source": "MANUAL/AUTO/SAMPLE", "reason": "..." }

# 查询欺诈检测结果
GET  /api/v1/fraud/result/{task_id}

# 更新黑名单
POST /api/v1/fraud/blacklist/add
Body: { "entity_name": "...", "entity_type": "ISSUER/ACCEPTOR/HOLDER", "reason": "..." }

# 查询历史欺诈案例（供参考）
GET  /api/v1/fraud/cases?keyword={keyword}&date_from={date}
```

---

### 3.11 批量调度 Agent（Batch Scheduling Agent）⭐ 新增

**职责定位**：管理大批量票据审核任务的入队、分发、并发控制与结果汇总，与 Orchestrator Agent 协作，支撑批量到期托收预审、批量贴现申请等大规模处理场景。

#### 3.11.1 核心能力

| 能力项 | 说明 |
|--------|------|
| 任务拆解 | 将批量文件包（ZIP/Excel 清单）拆解为单票独立子任务 |
| 优先级队列 | 高优先级（VIP 客户、紧急票据）插队，普通任务 FIFO |
| 并发控制 | 默认最大并发 100，可按资源池动态调整，防止下游服务过载 |
| 进度追踪 | 实时统计 已完成/进行中/失败 数量，支持进度百分比查询 |
| 失败重试 | 单票失败自动重试 3 次（指数退避），超限转人工队列 |
| 结果汇总 | 全部子任务完成后汇总为批量报告，支持增量导出（部分完成时） |
| 资源隔离 | 批量任务与单笔任务使用独立资源池，互不影响 |

#### 3.11.2 批量任务状态机

```
QUEUED → SPLITTING → DISPATCHING → PROCESSING（n/total）
       → PARTIAL_DONE（部分失败）→ RETRY_PENDING
       → COMPLETED（全部成功）
       → COMPLETED_WITH_ERRORS（含失败项）
       → FAILED（关键错误，整批失败）
```

#### 3.11.3 批量报告格式

```json
{
  "batch_id": "BATCH_20240115_001",
  "business_type": "COLLECTION_PRE_REVIEW",
  "submitted_at": "2024-01-15T06:00:00Z",
  "completed_at": "2024-01-15T06:28:32Z",
  "total": 500,
  "summary": {
    "passed": 423,
    "warning": 51,
    "failed": 22,
    "error": 4
  },
  "details": [
    {
      "bill_no": "310000000012345678",
      "status": "PASSED",
      "risk_level": "LOW",
      "report_url": "https://.../reports/..."
    }
  ],
  "expiry_alert_list": [
    { "bill_no": "...", "due_date": "2024-01-18", "days_remaining": 3, "holder": "A公司" }
  ]
}
```

#### 3.11.4 接口规范

```
# 提交批量任务
POST /api/v1/batch/submit
Body: { "business_type": "...", "files": [...], "priority": "NORMAL/HIGH" }

# 查询批量任务进度
GET  /api/v1/batch/{batch_id}/progress

# 获取批量结果（支持分页）
GET  /api/v1/batch/{batch_id}/results?page=1&size=50&status=FAILED

# 导出批量报告
GET  /api/v1/batch/{batch_id}/export?format=PDF|EXCEL
```

---

### 3.12 出票合规预检 Agent（Bill Issuance Compliance Agent）⭐ 新增

**职责定位**：在企业正式开具票据前，对出票人资格、出票要素格式、授信额度等进行前置合规核查，从源头防止不合规票据进入流通。

#### 3.12.1 核心检查项

| 检查项 | 数据来源 | 校验规则 |
|--------|----------|----------|
| 出票人营业执照 | 工商局查询接口 | 有效期内、未吊销/注销、经营范围含相关业务 |
| 出票人法定代表人 | 工商 + 行内 KYC 数据 | 与系统备案信息一致，无异常冻结状态 |
| 银行授信额度 | 行内核心授信系统 | 申请金额 ≤ 剩余可用授信额度 |
| 单张出票金额上限 | 监管规定 + 行内规则 | 商票单张上限 1000万，银票无单张限制但有余额限制 |
| 出票格式预验 | 规则引擎 | 18项必填要素提前预填校验，出票日期/到期日期合法性 |
| 历史拒付记录 | 行内黑名单 + 人行征信 | 近 2 年内有拒付记录，触发人工审核 |

#### 3.12.2 与贴现/承兑 Agent 的差异

出票合规预检发生在票据**尚未开具**时，目标是预防；贴现/承兑审核发生在票据**已流通**后，目标是合规放行。两者共用要素抽取 Agent，但调用的外部接口和校验规则集不同。

---

## 四、业务场景覆盖

### 4.1 场景一：贴现业务前审核

```
触发时机：企业申请票据贴现前
参与Agent：全量（Document Parser → Element Extraction → Compliance Check
            → Endorsement Chain → Contract Review → Risk Assessment → Report）
重点检查：
  - 贴现资格核查（持票人须为直接前手被背书人）
  - 贸易背景真实性（需提供合同 + 增值税发票）
  - 到期日校验（贴现日至到期日须 ≥ 7 天）
SLA：≤ 35秒/份
```

### 4.2 场景二：票据质押融资审核

```
触发时机：企业以票据为质押物申请贷款
参与Agent：Document Parser + Element Extraction + Compliance Check + Risk Assessment
重点检查：
  - 质押背书合规性（须注明"质押"字样）
  - 质押登记核查（人行征信系统）
  - 票据到期日须晚于贷款到期日
特殊处理：调用人行质押登记查询接口
SLA：≤ 60秒/份（含外部接口调用）
```

### 4.3 场景三：背书转让合规审核

```
触发时机：票据持有方发起背书转让操作
参与Agent：Document Parser + Endorsement Chain + Compliance Check
重点检查：
  - 新增背书的合规性
  - 背书人与当前持票人身份一致性
  - 是否存在"不得转让"标记
  - 转让日期合规性
SLA：≤ 15秒/份
```

### 4.4 场景四：批量到期票据托收预审

```
触发时机：每日 T-3 天自动触发到期票据批量预审
参与Agent：Document Parser + Element Extraction + Compliance Check
重点检查：
  - 提示付款期限合规性
  - 付款人账户状态
  - 委托收款手续完整性
处理量：单批 ≥ 500 份
SLA：批量任务 30 分钟内完成
```

### 4.5 场景五：可疑票据欺诈检测

```
触发时机：风控系统触发、人工举报、随机抽检
参与Agent：全量 + 专项反欺诈检测逻辑
重点检查：
  - 印章真伪识别（与历史印章库比对）
  - 票据号码重复查询（防双重贴现）
  - 关联方循环背书识别
  - OCR 置信度异常区域深度分析
处置：发现高风险直接冻结并通知合规部
SLA：不限时，以准确性优先
```

### 4.6 场景六：跨行业务实时流转监控（新增）

```
触发时机：
  1. 业务员主动查询 — 自然语言或票据号输入
  2. 运营大盘定时轮询 — 每 60 秒刷新全量在途业务
  3. 系统自动触发 — 某节点超时阈值到达时主动推送
  
参与 Agent：流转状态查询 Agent（独立运行，不依赖其他审核 Agent）

标准查询链路：
  业务员输入 → 意图识别 → 并发拉取各节点状态 → 卡点识别 → 超时计算
             → LLM 生成自然语言摘要 → 返回结构化时间轴 + 建议动作

覆盖业务：提示承兑、提示付款、贴现、背书转让、质押背书、撤销申请

关键输出：
  ✓ 全链路7步消息节点状态时间轴
  ✓ 当前卡点位置（精确到"哪个系统、等待多久"）
  ✓ 超时剩余时间 & 预警等级
  ✓ 异常码业务化解释
  ✓ 可操作的处置建议（含联系电话/操作入口）

SLA：单笔查询响应 ≤ 3 秒（含并发接口调用）
```

### 4.7 场景七：出票合规前置预检（新增）

```
触发时机：企业在银行柜台或网银申请开具银行/商业承兑汇票时
参与 Agent：出票合规预检 Agent + 合规检索 Agent + 要素抽取 Agent（预填格式校验）
重点检查：
  - 出票人营业执照有效性（工商接口实时核查）
  - 行内授信额度是否覆盖申请金额
  - 出票日期 / 到期日格式及期限合规性
  - 历史拒付记录查询（近 2 年）
  - 18 项出票要素预填格式预验
输出：合规预检报告 + 可开票额度确认 + 不合规项整改清单
SLA：≤ 20 秒（含工商接口调用）
```

### 4.8 场景八：可疑票据欺诈深度检测（新增）

```
触发方式：
  1. 风控系统自动信号（异常提交模式）
  2. 业务员人工举报
  3. 按比例随机抽检（默认 5%）
  4. 每周高金额票据（> 500 万）定期专项复查

参与 Agent：欺诈检测 Agent（核心）+ 背书链路 Agent + 合规检索 Agent + 报告生成 Agent

五维并行检测：
  印章真伪识别    → 相似度 < 0.75 直接冻结
  重复票据查询    → 全库去重，防双重贴现/承兑（分值直接置 1.0）
  图像篡改检测    → 笔迹压痕 + EXIF 元数据分析
  关联网络分析    → 循环背书 + 壳公司识别
  历史案例比对    → 黑名单 + 监管处罚记录

欺诈评分 > 0.70：冻结业务 + 保全证据 + 合规部介入 + 必要时上报监管
SLA：不限时，以检测准确性优先
```

### 4.9 场景九：逾期票据预警与处置辅助（新增）

```
触发时机：
  到期日前 T-3 天：自动扫描全量在途票据，生成到期预警清单
  到期当日：未收到承兑/付款确认的票据触发专项处置流程

参与 Agent：
  合规检索 Agent  → 逾期处置规则（追索权期限、公示催告程序）
  背书链路 Agent  → 还原完整背书链，确定追索顺序（末手→前手→出票人）
  风险评估 Agent  → 逾期损失评估 + 追索可行性评分
  报告生成 Agent  → 逾期处置方案建议（含法律依据、操作步骤、联系信息）

处置建议输出：
  第一追索路径：持票人提示付款 → 承兑人/出票人拒付 → 追索前手背书人
  法律手段建议：公示催告申请 / 票据诉讼 / 仲裁（含时效提醒）
  损失评估：当前市场贴现率下的损失估算

SLA：批量扫描（1000+ 张）≤ 60 分钟，单张逾期分析 ≤ 30 秒
```

### 4.10 场景十：撤销申请处理（新增）

```
触发时机：业务员发起撤销已提交的票据业务（仅限对手行未处理前）
参与 Agent：
  流转状态查询 Agent → 确认对手行当前处理状态（STEP 4 未完成才可撤销）
  合规检索 Agent     → 核查撤销时限及合规条件
  背书链路 Agent     → 确认无后续背书已发生
报文流程：发起行前置机 → 票交所发送撤销请求 → 对手行确认撤销
SLA：撤销合规校验 ≤ 10 秒，报文发送后等待对手行确认（最长 30 分钟）
```

### 4.11 场景十一：新员工辅助培训模式

```
触发时机：系统角色标识为"培训模式"的用户提交审核
特殊模式：在常规审核报告基础上增加"教学注解"覆盖层
  - 每项检查结果附带"为什么这样判定"的逻辑说明
  - 引用条款提供法规原文对照展示
  - 提供"如果不合规会有什么后果"的案例风险提示
  - 生成"审核要点速查卡"（适合打印随身携带）
目标：降低新员工上岗培训周期，理论与实操同步进行
```

---

## 五、非功能性需求

### 5.1 性能要求

| 指标 | 目标值 |
|------|--------|
| 单份票据端到端耗时 | ≤ 35 秒（P95） |
| 批量并发处理能力 | ≥ 100 份/次 |
| 系统可用性 | ≥ 99.9%（月均） |
| 向量检索耗时 | ≤ 200ms（P99） |
| API 响应时间 | ≤ 500ms（进度查询接口） |

### 5.2 安全与合规要求

- 票据影像数据传输全程 TLS 1.3 加密
- 存储加密（AES-256），影像文件单独加密存储
- 操作日志全量记录，不可篡改（存入 append-only 日志库）
- 数据访问基于 RBAC，最小权限原则
- 敏感数据（账号、金额）在日志中脱敏展示
- 审核结论与法规引用可追溯至具体版本的知识库快照

### 5.3 可观测性要求

- 每个 Agent 输出 Prometheus 指标：调用次数、耗时分位数、错误率
- Grafana Dashboard：任务处理大盘、Agent 健康状态、法规知识库覆盖度
- 全链路 Trace ID，支持跨 Agent 请求追踪（OpenTelemetry）
- 异常实时告警（邮件 + 企业微信）

### 5.4 可扩展性要求

- Agent 插件化设计：新增票据类型（如电子承兑汇票）只需新增对应 Agent 插件
- 知识库热更新：法规变更后无需重启服务即可更新向量库
- 模型可替换：底层 LLM 支持切换（Qwen / GPT / Claude），通过适配层屏蔽差异

---

## 六、Agent 间消息格式规范

### 6.1 统一消息 Schema

```json
{
  "message_id": "MSG_20240115_001_002",
  "task_id": "TASK_20240115_001",
  "from_agent": "ELEMENT_EXTRACTION",
  "to_agent": "COMPLIANCE_RETRIEVAL",
  "timestamp": "2024-01-15T10:30:00.123Z",
  "payload": {
    "status": "SUCCESS",
    "data": { /* Agent 具体输出 */ },
    "metadata": {
      "processing_time_ms": 1250,
      "model_version": "qwen2.5-14b-v1.2",
      "confidence": 0.94
    }
  },
  "errors": []
}
```

### 6.2 错误码规范

| 错误码 | 含义 | 处置策略 |
|--------|------|----------|
| E001 | OCR 识别失败 | 重试 3 次后转人工 |
| E002 | 要素抽取置信度过低 | 标记 LOW_CONFIDENCE，继续流程 |
| E003 | 向量检索无结果 | 扩大检索范围后重试 |
| E004 | LLM 调用超时 | 指数退避重试 |
| E005 | 外部接口不可用 | 跳过该项检查，报告中标注 |
| E006 | 背书链断裂 | 不阻断流程，标记 BROKEN |
| E999 | 未知错误 | 转人工处理 |

---

## 七、技术选型建议

| 组件 | 推荐方案 | 备选方案 |
|------|----------|----------|
| LLM 推理 | Qwen2.5-14B（私有化部署） | GPT-4o（API调用） |
| 向量数据库 | Milvus 2.3 | Weaviate / Qdrant |
| 任务队列 | Celery + Redis | RabbitMQ |
| Web 框架 | FastAPI | Django REST |
| OCR 引擎 | PaddleOCR | Tesseract + 商用API |
| 分块框架 | LangChain + 自定义策略 | LlamaIndex |
| 监控 | Prometheus + Grafana | Datadog |
| 链路追踪 | OpenTelemetry + Jaeger | Zipkin |
| 容器编排 | Kubernetes | Docker Compose（开发环境） |

---

## 八、里程碑与交付计划

| 阶段 | 时间 | 交付内容 |
|------|------|----------|
| Phase 1：基础能力 | M1-M2 | 文档解析 Agent + 要素抽取 Agent + Orchestrator 框架 |
| Phase 2：合规核心 | M2-M3 | 合规检索 Agent + 知识库入库（200+ 份文档） |
| Phase 3：背书与合同 | M3-M4 | 背书链路 Agent + 合同审核 Agent |
| Phase 4：风控集成 | M4-M5 | 风险评估 Agent + 报告生成 Agent |
| Phase 5：流转追踪 | M5-M5.5 | **流转状态查询 Agent**：前置机/票交所/对手行接口适配 + 超时预警引擎 + 自然语言查询 |
| Phase 6：安全与批量 | M5.5-M6 | **欺诈检测 Agent**（印章/篡改/关联网络）+ **批量调度 Agent**（Celery 资源池）|
| Phase 7：扩展场景 | M6-M7 | **出票合规预检 Agent** + 逾期预警处置 + 撤销申请 + 培训模式 + 全链路集成联调 |

---

*本文档为系统需求设计稿，具体实现细节以技术方案评审会议结论为准。*
