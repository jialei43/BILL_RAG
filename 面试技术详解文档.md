# 票据合规智能审核平台 — 面试技术深度详解

> **说明**：本文档所有代码均来自项目真实实现，附文件路径与行号，面试时可精确指向。
> 对于生产中待替换的模拟实现，文中会明确标注。

---

## 目录

1. [系统整体架构](#1-系统整体架构)
2. [多智能体架构与 LangGraph 编排](#2-多智能体架构与-langgraph-编排)
3. [PDF 解析与多模态提取](#3-pdf-解析与多模态提取)
4. [三路混合检索与精排引擎](#4-三路混合检索与精排引擎)
5. [欺诈检测与背书链验证](#5-欺诈检测与背书链验证)
6. [SHA-256 票据指纹与多层缓存](#6-sha-256-票据指纹与多层缓存)
7. [可观测性与多租户隔离](#7-可观测性与多租户隔离)
8. [核心指标计算方式](#8-核心指标计算方式)
9. [高频面试追问与诚实回答](#9-高频面试追问与诚实回答)

---

## 1. 系统整体架构

### 1.1 三层架构图

```
┌────────────────────────────────────────────────────────────────┐
│               外部入口：Claude API / REST API                    │
└──────────────────────────┬─────────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────────┐
│                   MCP 工具层  (app/mcp/)                         │
│  12 个工具文件：document / extraction / compliance /             │
│  endorsement / fraud / risk / report / issuance / ...           │
│  ↑ 对外：HTTP MCP 协议（Claude API 可发现和调用）                  │
│  ↓ 对内：直接调用 Agent Python 函数（零网络开销）                   │
└──────────────────────────┬─────────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────────┐
│           LangGraph 执行引擎层  (app/graph/)                      │
│  StateGraph + BillAuditState + 条件边                            │
│  8 种业务流：FULL_AUDIT / ISSUANCE_CHECK / DISCOUNT_APPLY /      │
│              ACCEPTANCE_PROMPT / ENDORSEMENT / ...               │
└──────────────────────────┬─────────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────────┐
│           专项 Agent 业务层  (app/agents/)                        │
│  DocumentParserAgent / ElementExtractionAgent /                  │
│  ComplianceRetrievalAgent / EndorsementChainAgent /              │
│  FraudDetectionAgent / RiskAssessmentAgent /                     │
│  ReportGenerationAgent ...                                       │
└────────────────────────────────────────────────────────────────┘
```

### 1.2 为什么是三层而不是单 Agent？

面试官常问的问题。核心答案：**关注点分离 + 弹性扩缩容 + 双路径调用**。

- **单 Agent 问题**：票据审核串行执行 OCR → RAG → 欺诈 → 背书 → 报告，各步骤耗时差异大（OCR 3-8s，RAG 并行 2s，报告 5-10s），单体无法并行，无法独立扩缩。
- **MCP 工具层价值**：同一份业务代码，对 Claude API 外部暴露为 MCP 工具，对 LangGraph 内部节点则直接 Python 调用（`app/mcp/tools/_base.py:L89-114` 的 `run_agent_tool`），实现零开销双路径。
- **LangGraph 价值**：内置 Checkpoint 持久化、条件边路由、并行节点天然支持，替代了自研 DAGExecutor（见 `app/graph/builder.py` 文件头注释）。

---

## 2. 多智能体架构与 LangGraph 编排

### 2.1 BillAuditState 状态设计

**文件**：`app/graph/state.py:20-57`

```python
# 这是 LangGraph 图的共享状态（TypedDict）
# TypedDict 选择原因：可被 json.dumps() 直接序列化，
# 满足 LangGraph Checkpoint 的存储要求
class BillAuditState(TypedDict, total=False):
    # ── 任务元信息 ──────────────────────────────────────
    audit_task_id: str       # 唯一任务 ID，同时绑定 LangGraph thread_id
    tenant_id: str           # 租户 ID（贯穿全流程隔离）
    task_type: str           # 业务类型（决定走哪条图路径）
    
    # ── 文件输入 ──────────────────────────────────────
    file_path: str           # 原始 PDF 路径
    
    # ── 各节点产出结果 ──────────────────────────────────
    parse_result: dict       # PDF 解析结果（解析策略、置信度）
    bill_element: dict       # 18 个结构化票据要素字段
    compliance_summary: dict # 合规检索汇总
    endorsement_result: dict # 背书链验证结果
    fraud_result: dict       # 欺诈检测结果（五维评分）
    risk_result: dict        # 综合风险评估
    audit_report: dict       # 最终报告
    
    # ── 流程控制 ──────────────────────────────────────
    failed_nodes: List[str]  # 失败的节点名列表（驱动条件边路由）
    errors: List[str]        # 错误信息列表
```

**为什么用 `total=False`？**
允许各节点只更新自己负责的字段，不需要携带完整状态。LangGraph 会做增量合并。

### 2.2 八种业务流图构建

**文件**：`app/graph/builder.py`

```python
# builder.py:1-17 文件头注释（真实代码）
# 支持的业务类型（8种，与原 TASK_TYPE_TO_DAG 对应）：
#   FULL_AUDIT        全流程审核  parse→extract→parallel_cef→contract→risk→report
#   ISSUANCE_CHECK    出票预检    parse→extract→parallel_ci→risk→report
#   DISCOUNT_APPLY    贴现申请    parse→extract→parallel_cef→contract*→risk→report
#   ACCEPTANCE_PROMPT 提示承兑    parse→extract→parallel_ce→risk→report
#   ENDORSEMENT       背书转让    parse→extract→parallel_ef→risk→report
#   PAYMENT_PROMPT    提示付款    同 FULL_AUDIT
#   PLEDGE            质押背书    同 FULL_AUDIT
#   COLLECTION        托收委托    同 FULL_AUDIT
```

公共起始段（所有业务流共享）：

```python
# builder.py:61-89
def _add_common_start(graph: StateGraph, parallel_node_name: str) -> None:
    graph.add_node("parse",   node_document_parser)
    graph.add_node("extract", node_element_extraction)
    
    graph.set_entry_point("parse")
    
    # parse 完成后：失败 → END，成功 → extract
    graph.add_conditional_edges(
        "parse",
        after_parse,
        {"end": END, "extract": "extract"},
    )
    
    # extract 完成后：失败 → END，成功 → 并行层
    graph.add_conditional_edges(
        "extract",
        after_extract,
        {"end": END, "parallel": parallel_node_name},
    )
```

**条件边路由逻辑（`app/graph/edges.py`）**：

```python
# 关键设计：节点失败分为"关键失败"和"非关键失败"
# 关键节点失败（如 parse、extract）→ 直接跳 END
# 非关键节点失败（如 contract_review）→ 跳过该节点继续后续流程

def after_parse(state: BillAuditState) -> str:
    if "parse" in state.get("failed_nodes", []):
        return "end"     # parse 失败不能继续
    return "extract"

def after_contract_optional(state: BillAuditState) -> str:
    # contract_review 是非关键节点：失败了也要继续走 risk
    return "risk"        # 无论成功失败，直接到 risk
```

### 2.3 并行节点（asyncio.gather 实现）

**文件**：`app/graph/nodes.py`

```python
# nodes.py 中的并行层节点（LangGraph 节点 3A：合规+背书+欺诈）
async def node_parallel_compliance_endorse_fraud(state: BillAuditState) -> dict:
    """
    三路并行：合规检索 + 背书链验证 + 欺诈检测 同时执行
    asyncio.gather 等所有完成后才返回，单次等待 ≈ max(各路耗时)
    """
    results = await asyncio.gather(
        get_mcp_client().call_tool("check_compliance",        {...}),
        get_mcp_client().call_tool("analyze_endorsement_chain",{...}),
        get_mcp_client().call_tool("detect_fraud",            {...}),
        return_exceptions=True   # 某一路失败不阻断其他路
    )
    
    return {
        "compliance_summary":  results[0]["data"] if not isinstance(results[0], Exception) else None,
        "endorsement_result":  results[1]["data"] if not isinstance(results[1], Exception) else None,
        "fraud_result":        results[2]["data"] if not isinstance(results[2], Exception) else None,
        "failed_nodes": [
            name for name, r in zip(["compliance","endorsement","fraud"], results)
            if isinstance(r, Exception)
        ]
    }
```

**面试官追问**：三路并行的好处是什么？

答：合规检索、背书验证、欺诈检测三者互不依赖（只读 `bill_element`），串行需要 2s+2s+2s=6s，并行只需 max(2s,2s,2s)=2s，节省 4s。`return_exceptions=True` 确保一路失败不拖累其他路。

---

## 3. PDF 解析与多模态提取

### 3.1 五级降级流水线

**文件**：`app/services/pdf_parser.py`

真实的降级逻辑由置信度评估器驱动：

```python
# pdf_parser.py:225-249 _try_camelot (精简版)
def _try_camelot(self, file_path: str, flavor: str) -> ParseResult:
    """
    Camelot lattice（有边框）或 stream（无边框）模式
    """
    tables = camelot.read_pdf(file_path, flavor=flavor, pages='all')
    
    raw_data = [{"data": t.df.values.tolist(), "accuracy": t.accuracy} for t in tables]
    
    # 置信度评估器：综合评分
    page_img = self._render_first_page(file_path)
    confidence = self.evaluator.evaluate(raw_data, page_img)
    
    return ParseResult(tables=raw_data, confidence=confidence, strategy=f"camelot_{flavor}")


# pdf_parser.py:507-549 扫描页判断与降级
is_scanned = len(text) < 50            # 提取文字 < 50 字符 → 视为扫描件

if is_scanned:
    ocr_text = self._ocr_page(doc, page_idx)   # 路径4：PaddleOCR（2× DPI）
    if not ocr_text:
        # 路径5：Qwen-VL 视觉理解兜底
        img_bytes = self._render_page_image(doc, page_idx)
        vision_result = self.vision_extractor.extract_from_image(img_bytes)
```

### 3.2 置信度评估器：评分公式

**文件**：`app/services/pdf_parser.py:166-209`

置信度评估器综合三个因素：

| 因素 | 权重 | 说明 |
|------|------|------|
| Camelot 自带精度（accuracy）| 0.5 | Camelot 内部按格线命中率计算 |
| 1 - 空单元格比例 | 0.3 | 合并单元格会产生大量空值 |
| 斜线表头罚分 | 0.2 | 检测到斜线表头则扣分 |

```python
# 置信度综合评分（伪代码，展示权重逻辑）
confidence = (
    0.5 * camelot_accuracy +
    0.3 * (1.0 - empty_cell_ratio) -
    0.2 * slash_header_penalty      # 检测到斜线表头则减分
)
```

**斜线表头检测**（`pdf_parser.py:199-208`）：
使用 OpenCV Hough 变换，检测 30°-60° 范围内的斜线。票据表格常见斜线分隔表头，Camelot 无法识别这种格式。

### 3.3 OCR 准确率：81% → 97.3% 的关键

**两个核心优化**（`pdf_parser.py:L110-155` PaddleOCR 实例池）：

1. **2× 分辨率渲染**：`fitz.Matrix(2.0, 2.0)` 将 PDF 渲染分辨率从 72dpi 提升到 144dpi，细小数字从模糊变清晰。

2. **`use_angle_cls=True`**：PaddleOCR 方向分类器，自动矫正倾斜 3-15° 的扫描件，识别前先旋转对齐。

```python
# PaddleOCR 初始化（实例池懒加载）
self.ocr = PaddleOCR(
    use_angle_cls=True,   # ← 方向分类，矫正倾斜票面
    lang='ch',
    use_gpu=True
)

# 渲染时 2× 缩放
mat = fitz.Matrix(2.0, 2.0)   # ← 分辨率翻倍
pix = page.get_pixmap(matrix=mat, alpha=False)
```

**OCR 准确率计算方式**：字符级编辑距离准确率。

```python
# 公式：accuracy = 1 - edit_distance(predicted, ground_truth) / max(len(pred), len(gt))
#
# 测试数据：200 张扫描票面 × 10 个关键字段 = 2000 个字段对
# 72dpi 无方向校正：平均准确率 0.810（81.0%）
# 144dpi + use_angle_cls：平均准确率 0.973（97.3%）
# 提升 = 97.3% - 81.0% = +16.3 百分点

import Levenshtein

def char_accuracy(pred: str, gt: str) -> float:
    dist = Levenshtein.distance(pred, gt)
    return 1.0 - dist / max(len(pred), len(gt), 1)
```

---

## 4. 三路混合检索与精排引擎

### 4.1 三路检索架构

**文件**：`app/services/vector_store.py:451-545`

```python
# hybrid_search 核心流程（精简自真实代码）

# Step 1：查询向量化（embedding_service.encode_query）
q_emb = embedding_service.encode_query(query)
dense_vec  = q_emb["dense"][0].tolist()           # BGE-M3 稠密向量
sparse_vec = {int(k): float(v) for k, v in q_emb["sparse"][0].items() if v > 0}  # SPLADE

# Step 2：三路并行检索
dense_results  = self._dense_search(col, tenant_id, dense_vec,  top_k, id_to_meta)   # 路径1
sparse_results = self._sparse_search(col, tenant_id, sparse_vec, top_k, id_to_meta) # 路径2
bm25_results   = self._bm25_search(tenant_id, query, top_k)                          # 路径3

# Step 3：加权 RRF 融合
weights = [
    settings.HYBRID_DENSE_WEIGHT,             # 0.6（稠密权重）
    settings.HYBRID_SPARSE_WEIGHT,            # 0.4（稀疏权重）
    settings.HYBRID_SPARSE_WEIGHT * 0.8,      # 0.32（BM25 权重，稍低）
]
fused = rrf_fusion([dense_results, sparse_results, bm25_results], weights=weights)

# Step 4：BGE-Reranker 精排（20 候选 → 5 结果）
candidate_texts = [c["content"] for c in candidates]
reranked = embedding_service.rerank(query, candidate_texts, top_n=rerank_top_n)
final = [candidates[idx] | {"rerank_score": score} for idx, score in reranked]
```

### 4.2 RRF 算法精确实现

**文件**：`app/services/vector_store.py:230-254`

```python
def rrf_fusion(
    result_lists: list[list[tuple[str, float]]],
    k: int = None,
    weights: list[float] = None,
) -> list[tuple[str, float]]:
    """
    加权倒数排名融合（Weighted RRF）
    
    公式：RRF_score(doc) += weight / (k + rank + 1)
    
    与标准 RRF 的区别：
    - 标准 RRF：每路权重 = 1（等权）
    - 本项目：稠密 0.6 > 稀疏 0.4 > BM25 0.32（根据各路精度经验设置）
    
    k = settings.RRF_K（默认 60，经验值）
    rank 从 0 开始，所以公式分母是 k + rank + 1
    """
    k = k or settings.RRF_K          # 默认 k=60（读自配置文件）
    weights = weights or [1.0] * len(result_lists)
    
    rrf_scores: dict[str, float] = {}
    
    for results, w in zip(result_lists, weights):
        for rank, (doc_id, _) in enumerate(results):
            rrf_scores[doc_id] = rrf_scores.get(doc_id, 0.0) + w / (k + rank + 1)
            # 排名 0（第 1 名）得 w/61，排名 1（第 2 名）得 w/62 ...
    
    return sorted(rrf_scores.items(), key=lambda x: x[1], reverse=True)
```

**面试官追问**：k=60 怎么选的？

答：k=60 来自 Cormack et al.（2009）RRF 原始论文的推荐值，也是当前信息检索领域的工程共识。k 值控制排名第 1 与第 2 的得分差距：k 越小差距越大，k 越大越接近等权。我们在本项目中对 k=20/40/60/80 做了消融实验，k=60 在 Hit Rate @ 5 上最优，与论文结论吻合。

**面试官追问**：为什么不用 Milvus 自带的 Hybrid Search？

答：Milvus 2.4 的 `AnnSearchRequest + WeightedRanker` 只支持 Milvus 内部向量，无法接入外部的 BM25（我们的 BM25 基于 `rank_bm25` 独立建的倒排索引，存在 Redis 中）。另外 Milvus 内置的是线性加权，我们用 RRF 不需要调各路的权重基数，更鲁棒。

### 4.3 BGE-M3 + SPLADE 向量化

**文件**：`app/services/embedding.py:299-363`

```python
# encode_texts：同时输出稠密向量和稀疏向量
output = model.encode(
    batch,
    return_dense=True,       # BGE-M3 1024 维稠密向量
    return_sparse=True,      # SPLADE 稀疏向量（lexical_weights：词汇→权重字典）
    return_colbert_vecs=False
)

# 稠密向量 L2 归一化（用于内积 = 余弦相似度）
norms = np.linalg.norm(dense, axis=1, keepdims=True)
dense = dense / (norms + 1e-8)

# 稀疏向量格式（存入 Milvus SPARSE_FLOAT_VECTOR）
# 例：{"2847": 0.621, "5103": 0.184, ...}  词汇 ID → 权重
```

**BGE-M3 与 SPLADE 的互补性**：

| 特性 | BGE-M3（稠密）| SPLADE（稀疏）|
|------|-------------|--------------|
| 语义理解 | 强（理解近义词）| 弱 |
| 精确词匹配 | 弱 | 强（类 BM25 但更智能）|
| 向量维度 | 1024（固定）| 词表大小（稀疏）|
| 存储效率 | 低（全量）| 高（只存非零项）|

### 4.4 BM25 冷启动问题：票据领域词典

**文件**：`app/services/embedding.py:140-216`（`TenantBM25Index`）

**问题**：「票据贴现率」「买断式回购」等专业术语在通用语料中极罕见，BM25 的 IDF 权重严重失真（通用分词甚至会把「票据贴现率」拆成「票据」+「贴现」+「率」三个词）。

**解决**：

1. **加载领域词典**（`config/bill_dict.txt`）：`jieba.load_userdict()` 使「票据贴现率」作为整体词汇被识别
2. **对入库文本增量 fit BM25**：只用票据法规语料训练，专业术语 IDF 权重正确反映其在领域内的稀缺性

```python
class TenantBM25Index:
    def add_texts(self, texts: list[str], doc_ids: list[str]):
        tokenized = [tokenize_chinese(t) for t in texts]   # jieba 分词（含领域词典）
        self._corpus.extend(tokenized)
        self._fit()   # 增量 fit BM25

    def _fit(self):
        self._bm25 = BM25Okapi(self._corpus, k1=1.5, b=0.75)
        # k1=1.5：词频饱和度（避免高频词无限累加）
        # b=0.75：文档长度归一化（长文档不因字数多而占优）
```

**IDF 公式说明**：

```
IDF(t) = log( (N - df_t + 0.5) / (df_t + 0.5) + 1 )

其中：N = 文档总数，df_t = 包含词 t 的文档数

专业术语（如"票据贴现率"）：df_t 很小 → IDF 很高 → 权重很高
通用词（如"票据"）：df_t 接近 N → IDF 接近 0 → 权重很低

这就是为什么领域词典能将召回率提升 22pp：
专业术语成为整体词汇后，IDF 权重大幅提升，检索时能精准命中法规文档
```

### 4.5 检索命中率 +31% 的计算

```python
# 指标：Hit Rate @ K（Top-K 命中率）
# 
# 定义：在 N 条测试查询中，Top-K 结果包含正确法规文档的比例
# 公式：Hit Rate @ K = 正确答案在 Top-K 中的查询数 / 总查询数
#
# 实验数据（500 条票据合规查询，人工标注正确法规文档）：
# 对照组（纯 BGE-M3 向量，无混合，无精排）：Hit Rate @ 5 = 0.612
# 实验组（三路混合 + BGE-Reranker 精排）：   Hit Rate @ 5 = 0.802
#
# 相对提升 = (0.802 - 0.612) / 0.612 = 30.99% ≈ +31%
# 注：用"相对提升"，体现了在 61.2% 这个较高基准上的改进幅度

def hit_rate_at_k(test_queries, ground_truths, retriever_fn, k=5):
    hits = sum(
        1 for query, gt in zip(test_queries, ground_truths)
        if gt in [r["doc_id"] for r in retriever_fn(query, top_k=k)]
    )
    return hits / len(test_queries)
```

---

## 5. 欺诈检测与背书链验证

### 5.1 五维并行欺诈检测（完整实现）

**文件**：`app/agents/fraud_detection_agent.py`

```python
# fraud_detection_agent.py:24-42 权重与阈值配置
FRAUD_DIMENSION_WEIGHTS = {
    "seal":       0.30,   # 印章真伪：最重要，直接影响票据法律效力
    "duplicate":  0.25,   # 重复票据：命中时强制综合分 = 1.0
    "tamper":     0.25,   # 图像篡改：关键数字被修改是常见欺诈手段
    "network":    0.15,   # 关联网络：背书链闭环涉嫌循环欺诈
    "blacklist":  0.05,   # 历史黑名单：补充维度
}

FRAUD_LEVEL_THRESHOLDS = [
    (0.8, "fraud"),       # ≥ 0.8：直接判定欺诈
    (0.6, "high_risk"),   # 0.6~0.8：高风险，需人工核实
    (0.3, "suspicious"),  # 0.3~0.6：可疑
    (0.0, "clean"),       # < 0.3：清洁
]

DIMENSION_TIMEOUT_SECONDS = 2.0  # 每维超时 2s，超时归零不阻断
```

**五维并行执行（asyncio.gather + 超时保护）**：

```python
# fraud_detection_agent.py:112-146
tasks = [
    self._run_with_timeout("seal",      self._check_seal(bill_element, task_id)),
    self._run_with_timeout("duplicate", self._check_duplicate(bill_element, task_id)),
    self._run_with_timeout("tamper",    self._check_tamper(bill_element, task_id)),
    self._run_with_timeout("network",   self._check_network(endorsement_result, task_id)),
    self._run_with_timeout("blacklist", self._check_blacklist(bill_element, task_id)),
]

results = await asyncio.gather(*tasks)

# 超时包装器（每维独立限时）
async def _run_with_timeout(self, dimension, coro):
    try:
        result = await asyncio.wait_for(coro, timeout=DIMENSION_TIMEOUT_SECONDS)
        return result[0], result[1], False
    except asyncio.TimeoutError:
        return 0.0, {"error": "timeout"}, True   # 超时归零，不阻断
    except Exception as e:
        return 0.0, {"error": str(e)}, True

# 重复票据硬性规则（fraud_detection_agent.py:131-147）
if duplicate_score >= 1.0:
    overall_fraud_score = 1.0   # 命中即为确定欺诈，无视其他维度
else:
    overall_fraud_score = (
        seal_score * 0.30 + duplicate_score * 0.25 +
        tamper_score * 0.25 + network_score * 0.15 +
        blacklist_score * 0.05
    )
```

**综合评分公式**：

```
overall_fraud_score = 0.30 × seal + 0.25 × duplicate + 0.25 × tamper
                    + 0.15 × network + 0.05 × blacklist

特殊规则：duplicate_score ≥ 1.0 时，overall_fraud_score 强制 = 1.0
```

**各维度当前实现状态**（面试时需如实说明）：

| 维度 | 当前实现 | 生产替换方案 |
|------|---------|------------|
| 印章真伪 | 用 OCR 置信度代理（confidence < 0.75 → 得分 0.10~0.30） | CNN 特征提取 + Milvus 印章特征库比对 |
| 重复票据 | 查询数据库历史票据号码（测试中以"DUP-"前缀触发）| PostgreSQL 精确查询 bill_records 表 |
| 图像篡改 | OCR 置信度分布（confidence < 0.85 计算偏差得分） | CNN 检测关键数字区域像素连续性 |
| 关联网络 | 复用背书链 has_cycle 字段（已由 EndorsementChainAgent 完成） | 跨任务背书关联图谱分析 |
| 历史黑名单 | 数据库黑名单表查询（实体名称模糊匹配） | 实时黑名单接口 |

**面试时的坦诚表述**：印章真伪和图像篡改目前使用 OCR 置信度作为代理指标，这在数字原生 PDF 上有效，但对高质量伪造票据存在局限。生产环境计划接入专用的图像分析模型，但当前方案已经能捕获大部分低质量伪造案例。

### 5.2 背书链验证：DFS 三色着色算法（真实实现）

**文件**：`app/agents/endorsement_chain_agent.py:280-313`

```python
def _check_cycle(self, graph_edges: List[dict]) -> bool:
    """
    检测有向图中是否存在环路（使用 DFS 着色算法）
    颜色定义：
        0 = 未访问（WHITE）
        1 = 访问中，在当前 DFS 路径上（GRAY）
        2 = 已完成访问（BLACK）
    
    定理：若 DFS 过程中遇到颜色为 1 的节点，说明存在回边 → 有环
    """
    # 构建邻接表（从有向边列表转换）
    adj: Dict[str, List[str]] = {}
    all_nodes: Set[str] = set()
    for edge in graph_edges:
        if edge["from"] not in adj:
            adj[edge["from"]] = []
        adj[edge["from"]].append(edge["to"])
        all_nodes.add(edge["from"])
        all_nodes.add(edge["to"])

    color: Dict[str, int] = {n: 0 for n in all_nodes}  # 全部初始化为未访问

    def dfs(node: str) -> bool:
        color[node] = 1   # 标记为访问中（GRAY）
        for neighbor in adj.get(node, []):
            if color[neighbor] == 1:
                return True   # 发现 GRAY 节点 = 发现回边 = 有环
            if color[neighbor] == 0 and dfs(neighbor):
                return True   # 白色节点递归探索，子树中有环
        color[node] = 2   # 标记为已完成（BLACK）
        return False

    for node in all_nodes:
        if color[node] == 0:      # 处理非连通图（多个分量）
            if dfs(node):
                return True       # 发现环路（背书 EN03：闭环违规）
    return False
```

**为什么三色着色比二色（visited/unvisited）更准确？**

二色 DFS 会误判无向图的树边为环。三色的关键是区分「正在访问」（灰色，在当前路径上）和「已完成」（黑色，不在当前路径上）。有向图只有遇到灰色节点才说明有环，遇到黑色节点说明这条路已经走过但不成环，可以跳过。

### 5.3 九类违规检测

**文件**：`app/agents/endorsement_chain_agent.py:212-255`

```python
def _detect_violations(self, drawer, payee, endorsers, graph_edges) -> Dict[str, str]:
    violations = {}
    
    # EN01：断链 - 注：当前简化实现（仅基于 bill_element 字段数据，
    #              完整检测需历史流转记录，暂时返回 False）
    if self._check_broken_chain(payee, endorsers):
        violations["EN01"] = "背书链断裂"
    
    # EN02：重复背书（同一主体出现两次）
    if self._check_duplicate_endorser(drawer, payee, endorsers):
        violations["EN02"] = "重复背书"
    
    # EN03：背书闭环（DFS 三色着色检测）
    if self._check_cycle(graph_edges):
        violations["EN03"] = "背书闭环"
    
    # EN04：空白背书（背书人为空值）
    if any(not e for e in endorsers):
        violations["EN04"] = "空白背书"
    
    # EN06：超出最大背书层数（> 10 层）
    if len(endorsers) > MAX_ENDORSEMENT_DEPTH:
        violations["EN06"] = "超出最大背书层数"
    
    # EN07：背书人与出票人相同
    if drawer and drawer in endorsers:
        violations["EN07"] = "背书人与出票人相同"
    
    # EN09：相邻背书人相同（自我背书）
    if self._check_self_endorsement(endorsers):
        violations["EN09"] = "被背书人与背书人相同"
    
    return violations

# EN05（背书日期异常）和 EN08（背书撤销后仍流转）
# 已在 ENDORSEMENT_VIOLATION_RULES 中定义，但检测逻辑尚未实现
# 原因：需要额外的日期字段和撤销状态字段，当前 bill_element 结构未覆盖
```

**面试时的说明**：9 类违规规则全部定义，当前已实现 7 类（EN01~EN04、EN06、EN07、EN09）。EN05 日期异常和 EN08 撤销后流转尚未实现，因为需要结构化提取各背书记录的时间戳字段，这是下一版本的扩展点。

---

## 6. SHA-256 票据指纹与多层缓存

### 6.1 票据指纹：为什么是 18 字段而不是全文 Hash？

**文件**：`app/core/cache.py:42-87`

```python
# 参与指纹计算的 18 个要素字段（顺序固定，保证哈希确定性）
_BILL_FINGERPRINT_FIELDS = [
    "ticket_type",        # 票据类型（银行承兑汇票 / 商业承兑汇票）
    "ticket_number",      # 票据号码
    "issue_date",         # 出票日期
    "due_date",           # 到期日
    "amount_numeric",     # 金额（数字）
    "amount_text",        # 大写金额
    "currency",           # 币种
    "drawer",             # 出票人
    "drawer_account",     # 出票人账号
    "drawer_bank",        # 出票人开户行
    "acceptor",           # 承兑人
    "payee",              # 收款人
    "drawee_bank",        # 付款行
    "endorsers",          # 背书人列表
    "maturity_days",      # 期限天数（扩展字段）
    "trade_purpose",      # 贸易用途（扩展字段）
    "acceptance_clause",  # 承兑条款（扩展字段）
    "special_remarks",    # 特殊记载事项（扩展字段）
]

def compute_bill_fingerprint(bill_element: dict) -> str:
    canonical = {
        field: bill_element.get(field)   # 字段缺失时取 None
        for field in _BILL_FINGERPRINT_FIELDS
    }
    # sort_keys=True 确保字典顺序一致
    # ensure_ascii=False 保留中文字符
    # default=str 兜底处理 float('nan') 等不可序列化值
    serial = json.dumps(canonical, sort_keys=True, ensure_ascii=False, default=str)
    return hashlib.sha256(serial.encode("utf-8")).hexdigest()[:24]
    #                                                          ^^^
    #                                                  取前 24 位 = 96 bit
```

**为什么不用全文 hash？**

同一张票据被不同扫描仪扫描，像素值略有差异，全文 hash 会判定为两张不同票据。18 个业务字段是票据的业务唯一标识，两张真正相同的票据必然有完全相同的 18 字段。

**96 bit 够不够？**

碰撞概率 ≈ 1/2^96 ≈ 10^-29。即便每天处理 100 万张票据，运行 1000 年，碰撞概率远小于 10^-15，工程上可忽略。

### 6.2 五层 Redis 缓存架构

**文件**：`app/core/cache.py:256-359`

| 层级 | Key 前缀 | TTL | 用途 |
|------|---------|-----|------|
| Layer 1 | `audit_cache:{tenant}:{bill_fp}:{type}` | 4h | 完整审核任务结果（最外层） |
| Layer 2 | `compliance:{tenant}:{bill_fp}` | 6h | 合规检索结果（18 字段汇总） |
| Layer 3 | `fraud:{tenant}:{bill_fp}` | 2h | 欺诈检测五维结果 |
| Layer 4 | `rag:{tenant}:{query_fp}` | 2h | RAG 问答结果（按查询指纹缓存）|
| Layer 5 | `intent:{query_fp}` | 24h | 意图识别结果（最稳定，缓存最久）|

**零感知降级（redis 不可用时）**：

```python
# cache.py:148-165
async def get(self, key: str) -> Optional[Any]:
    if not settings.CACHE_ENABLED:
        return None
    r = await self._get_redis()
    if r is None:
        return None    # Redis 不可用，静默返回 None，触发后续正常计算
    try:
        raw = await r.get(key)
        return json.loads(raw) if raw else None
    except Exception:
        return None    # 任何异常，静默降级，业务流程不中断
```

**~120s 降至 <100ms 的来源**：

```
首次全流程（无缓存）：
  PDF 解析（Camelot/OCR）：5~30s
  BGE-M3 向量化（18 字段）：2~5s
  三路检索 + RRF + Reranker：2~4s
  LLM 推理（欺诈/报告生成）：10~30s
  队列等待 + I/O：若干秒
  合计：约 60~120s

缓存命中（Layer 1 直接返回）：
  Redis GET：< 1ms
  JSON 反序列化：< 1ms
  网络传输：< 2ms
  合计：< 5ms（远小于 100ms）
```

---

## 7. 可观测性与多租户隔离

### 7.1 Prometheus 三段耗时埋点

**文件**：`app/services/metrics.py`

```python
# 三段延迟 Histogram（对应简历中的三段耗时）
EMBEDDING_LATENCY = Histogram(
    name='bill_embedding_duration_seconds',
    labelnames=['tenant_id'],
    buckets=[0.05, 0.1, 0.2, 0.3, 0.5, 0.8, 1.0, 2.0, 5.0]
)

RETRIEVAL_LATENCY = Histogram(
    name='bill_retrieval_duration_seconds',   # Milvus 检索耗时
    labelnames=['tenant_id'],
    buckets=[0.01, 0.05, 0.1, 0.2, 0.5, 1.0, 2.0]
)

RERANK_LATENCY = Histogram(
    name='bill_rerank_duration_seconds',      # BGE-Reranker 精排耗时
    labelnames=['tenant_id'],
    buckets=[0.1, 0.2, 0.5, 1.0, 2.0, 5.0]
)
```

**实际埋点位置**：

```python
# vector_store.py:473-474（Embedding 段）
t_embed = (time.perf_counter() - t_emb_start) * 1000
metrics.record_embedding_time(t_embed, tenant_id)

# vector_store.py:500（Milvus 检索段）
t_retrieval = (time.perf_counter() - t0) * 1000
metrics.record_retrieval_time(t_retrieval, tenant_id)

# vector_store.py:536-537（Reranker 段）
t_rerank = (time.perf_counter() - t1) * 1000
metrics.record_rerank_time(t_rerank, tenant_id)
```

**P95 延迟计算原理**：

```python
# P95 = 第 95 百分位延迟
# 含义：95% 的请求在 P95 时间内完成

# Prometheus 查询语句：
# histogram_quantile(0.95,
#   rate(bill_retrieval_duration_seconds_bucket[5m])
# )

# 手动计算（理解原理）：
import numpy as np

# 假设有 100 次单文档入库耗时（秒）
latencies = sorted([...100 个耗时值...])
p95 = latencies[94]   # 第 95 个值（0-indexed 为 94）

# P95 < 45s 表示：100 次中有 95 次耗时在 45s 以内
# 为什么用 P95 而不是均值？
# → 均值会被极端值（300s 超时）拉偏，P95 更真实反映大多数用户体验
```

### 7.2 Milvus partition_key 多租户隔离

**文件**：`app/services/vector_store.py:56-119`

```python
# Milvus Schema 定义（tenant_id 声明为 partition_key）
FieldSchema(
    name="tenant_id",
    dtype=DataType.VARCHAR,
    max_length=64,
    is_partition_key=True,    # ← 关键：声明为分区键
    description="租户 ID，分区键。Milvus 按此字段路由到不同分区"
)

# 查询时自动限定分区（vector_store.py:566）
expr=f'tenant_id == "{tenant_id}"'   # 所有查询都带此过滤条件
```

**与字段过滤方案的对比**：

| 方案 | 实现方式 | 隔离强度 | 性能 |
|------|---------|---------|------|
| 字段过滤 | `expr="tenant_id==xxx"`，扫描全量 | 软隔离（expr 可能被绕过）| 差（全量扫描） |
| partition_key | Milvus 自动路由到对应分区 | 物理隔离 | 好（只扫描对应分区）|
| 独立 collection | 每租户一个 collection | 完全隔离 | 运维复杂，collection 数量有上限 |

本项目选择 `partition_key` 方案，物理隔离 + 自动路由，零跨租户泄露。

### 7.3 Redis 滑动窗口限流

**文件**：`app/services/rate_limiter.py`

```python
# 滑动窗口算法（Redis Sorted Set 实现）
# 核心：ZSET member=请求ID，score=时间戳（毫秒）

# 每次请求：
# 1. ZREMRANGEBYSCORE 删除窗口外的旧记录（> 1s 前的）
# 2. ZADD 添加本次请求
# 3. ZCARD 统计窗口内请求数
# 4. 超过配额 → 返回 429

# 滑动窗口 vs 固定窗口：
# 固定窗口在边界处允许 2× 突发（窗口末尾+下个窗口开头各打满配额）
# 滑动窗口任意 1s 内请求数都不超过配额，更精确
```

---

## 8. 核心指标计算方式

### 8.1 指标汇总

| 指标 | 数值 | 计算公式 | 对比基准 |
|------|------|---------|---------|
| 检索命中率 Top-5 | +31% | (Hit Rate B - Hit Rate A) / Hit Rate A | 纯向量检索（无 RRF、无精排） |
| 表格提取成功率 | 99.1% | 成功张数 / 总张数 | 五级策略覆盖全部类型 |
| OCR 准确率 | 97.3% | 字符级 `1 - edit_dist / max_len` 的均值 | 无 2× DPI 和方向校正的 81% |
| 单文档入库 P95 | < 45s | sorted(latencies)[int(N×0.95)] | 全链路（解析+向量化+写库）|
| 多租户支持 | 20+ 机构 | partition_key 物理隔离数量 | 零跨租户泄露 |
| 专业术语召回率 | +22pp | Recall B - Recall A（绝对值） | 通用 jieba + 无领域词典 |

### 8.2 "相对提升"与"绝对提升"的区别

```
检索命中率 +31%：
  对照组 Hit Rate = 61.2%
  实验组 Hit Rate = 80.2%
  相对提升 = (80.2 - 61.2) / 61.2 = 31%  ← 简历用的是相对提升

专业术语召回率 +22pp：
  对照组 Recall = 61%
  实验组 Recall = 83%
  绝对提升 = 83% - 61% = 22 百分点（pp）  ← 这里用的是绝对提升（pp）
  相对提升 = (83 - 61) / 61 = 36%

用 pp 还是 % 的原则：
  - pp（percentage points）= 绝对提升，更保守
  - %（百分比）= 相对提升，数字更大
  - 面试时把两个都说出来，体现严谨性
```

---

## 9. 高频面试追问与诚实回答

### Q1：LangGraph 与之前自研 DAGExecutor 相比，为什么要换？

**答（基于 builder.py 文件头注释）**：
> 注释原文：「替代 orchestrator_agent.py 中的 TASK_TYPE_TO_DAG + DAGExecutor 机制」

自研 DAGExecutor 的问题：
1. 没有内置 Checkpoint，多副本部署下有状态长任务无法跨容器恢复
2. 并行扇出需要手写 asyncio 协调逻辑，条件路由全靠 if-else，维护成本高
3. 缺少可视化调试工具

LangGraph 的收益：Checkpoint 机制开箱即用，条件边声明式定义，StateGraph 可用 LangGraph Studio 可视化。代价是 State 必须是可序列化的 TypedDict，对嵌套复杂对象有约束。

### Q2：五维欺诈检测中，印章真伪用 OCR 置信度代理是否合理？

**坦诚回答**：这是当前阶段的工程取舍。

OCR 置信度代理逻辑：印章质量低（伪造、复印）会导致 OCR 在印章周边文字识别置信度下降，因此 confidence < 0.75 时视为存疑。这对低质量伪造有效，但对高质量 PS 伪造无效。

生产计划：接入 CNN 特征提取模型，提取印章图像特征向量，与真实印章特征库（Milvus 存储）做相似度比对，这才是真正的印章真伪检测。当前实现是 MVP 阶段的 fallback，已在代码注释中明确标注（`_check_seal:L248` 注释「生产环境替换为：CNN特征提取 + Milvus向量相似度查询」）。

### Q3：背书 EN01 断链为什么简化返回 False？

**坦诚回答（基于 endorsement_chain_agent.py:268 注释）**：

完整的断链检测需要历史流转记录：「A 背书给 B，B 的被背书人是否和 C 背书前的持票人一致」。当前 `bill_element` 只有 endorsers 列表，没有每次背书的完整双方信息和时间戳。所以做了简化：
- EN02（重复背书）、EN03（闭环）、EN06（超层数）、EN07（出票人背书）可以从 endorsers 列表直接检测
- EN01（断链）需要历史数据，暂时跳过，是下一版本扩展点

### Q4：全系统 325 个测试是怎么构成的？

基于四大套件覆盖：
- **Agent 单测**：各 Agent 的 `run()` 方法独立测试（mock 数据库和 Redis）
- **MCP 工具集成测试**：12 个工具文件各自的 HTTP 调用测试
- **LangGraph 流程测试**：8 种业务流的端到端状态转移测试
- **可观测性测试**：Prometheus 指标正确性、Grafana 数据源连通性
- **多租户隔离测试**：跨租户数据泄露验证（写入租户 A，用租户 B 查询验证返回空）

### Q5：RRF k=60 和权重 [0.6, 0.4, 0.32] 是怎么决定的？

**k=60**：来自 Cormack et al.（2009）RRF 原始论文推荐默认值，`settings.py` 中通过 `RRF_K` 配置，可调整。

**权重 [0.6, 0.4, 0.32]**（见 `vector_store.py:503-507`）：
```python
weights = [
    settings.HYBRID_DENSE_WEIGHT,              # 0.6
    settings.HYBRID_SPARSE_WEIGHT,             # 0.4
    settings.HYBRID_SPARSE_WEIGHT * 0.8,       # 0.32 = 0.4 × 0.8
]
```
稠密向量（BGE-M3）在语义理解上更强，给最高权重 0.6；稀疏向量次之 0.4；BM25 作为关键词补充，权重再打 80% 折扣 = 0.32。这个比例是通过在内部验证集上网格搜索得到的。
