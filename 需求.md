# 票据 RAG 系统功能需求文档 v1.0

> 基于现有 `BillElement` 识别结构、`VectorStore` 三路混合检索、`QueryLog` 日志表，
> 本文档描述系统从"识别票据"到"联合 RAG 回答"的完整闭环需求。

---

## 一、前置需求（系统基础能力，须优先实现）

---

### P1 票据生命周期管理（上传 → 识别 → 入库 → 流转增量更新）

**背景**：当前 `Document` 表将每次上传视为独立文档，无法识别"同一张票据在不同流转阶段被多次上传"的情况。票据在背书转让、托收过程中会新增背书人记录，需要在不删除原有向量的前提下追加更新。

**需新增数据表**：

```
BillRecord（票据主档表）
  - id:              UUID 主键
  - tenant_id:       所属租户（外键 → tenants.id）
  - ticket_number:   票据号码（唯一业务键，同一票号视为同一票据）
  - ticket_type:     票据类型（银行承兑汇票 / 商业承兑汇票 / 本票）
  - issue_date:      出票日期
  - due_date:        到期日
  - amount_numeric:  票面金额（数字）
  - amount_text:     大写金额
  - currency:        币种
  - drawer:          出票人
  - drawer_account:  出票人账号
  - drawer_bank:     出票人开户行
  - acceptor:        承兑人
  - payee:           收款人（初始收款人）
  - drawee_bank:     付款行
  - risk_flags:      JSON，矛盾/风险标记列表
  - latest_version:  当前最新版本号
  - created_at / updated_at

BillVersion（票据流转版本表）
  - id:              UUID 主键
  - bill_record_id:  外键 → bill_record.id
  - version:         版本号（从 1 递增）
  - document_id:     对应的 Document.id（原始文件记录）
  - endorsers:       JSON 数组，本次识别到的完整背书人列表
  - new_endorsers:   JSON 数组，相比上一版本新增的背书人
  - upload_time:     本次上传时间
  - uploaded_by:     操作人 user_id
  - recognition_raw: JSON，BillElement 原始识别结果（调试备用）
```

**上传→入库流程**：

```
用户上传票据图片/PDF
         ↓
BillRecognitionService.recognize_file() → BillElement
         ↓
以 ticket_number 查询 BillRecord 是否存在
  ├── 不存在（新票据）：
  │     创建 BillRecord（写入所有识别字段）
  │     创建 BillVersion（version=1，endorsers=识别结果）
  │     触发 P1-A：结构化票据信息向量化入库（见下）
  └── 已存在（票据流转）：
        对比新旧 endorsers 差异 → 提取 new_endorsers
        创建 BillVersion（version+1）
        触发 P1-B：仅追加新增背书人的向量块，不删除旧版本向量
         ↓
返回：{ bill_record_id, version, is_new_bill, new_endorsers }
```

**P1-A：结构化票据信息向量化入库**：

将 `BillElement` 的每个字段组合成自然语言块，写入 Milvus，`chunk_type` 标记为 `bill_element`：

```
示例向量块内容：
"票据号码 EA2024001234 的银行承兑汇票，
 出票人：XX贸易有限公司（账号：622848...，开户行：中国工商银行杭州分行），
 承兑人：杭州银行股份有限公司，付款行：杭州银行总行营业部，
 出票日期：2024-09-15，到期日：2025-03-15，
 票面金额：人民币壹佰万元整（￥1,000,000.00）,
 收款人：YY科技有限公司，背书人：[丙实业, 丁物流]"
```

**P1-B：流转增量向量化**：

```
仅新增背书人条目的向量块：
"票据号码 EA2024001234 第3次流转背书（2025-01-10），
 新增背书人：戊金融服务有限公司，
 当前完整背书链：[丙实业 → 丁物流 → 戊金融服务有限公司]"
```

**为什么是前置需求**：后续所有场景的专项检索，都依赖以 `ticket_number` 为主键能查到票据全生命周期数据；没有此表，场景三（追索权时效）、场景六（流转溯源）无法落地。

---

### P2 意图识别与专项检索路由引擎

**背景**：当前 `rag_service.py` 直接将用户原始问题送入向量检索，无法区分"查利率"和"查背书链合规"，导致检索结果混乱。需要在检索前增加意图分类层，将用户问题路由到对应的专项检索处理器。纯关键词匹配无法覆盖用户的自然语言表达变体（如"这张票快到期了怎么办"应识别为 `INTENT_EXPIRY`，但不含任何关键词），因此**必须结合 qwen-max 模型进行语义级意图理解**，关键词规则仅作为前置快速通道。

**需新增组件**：`IntentRouter`（意图路由器）

**意图分类表**（初始版本，可动态扩展）：

| 意图 ID | 意图名称 | 典型关键词（快速通道） | 路由到的专项场景 |
|---|---|---|---|
| `INTENT_PRICING` | 贴现定价查询 | 利率、定价、贴现、报价、折扣 | 场景一 |
| `INTENT_ENDORSER_CHECK` | 背书链核查 | 背书、背书人、背书链、连续、签章 | 场景二 |
| `INTENT_EXPIRY` | 到期追索查询 | 到期、追索权、时效、逾期、拒付、付款 | 场景三 |
| `INTENT_AML` | 大额合规查询 | 大额、反洗钱、报告义务、AML、尽职调查 | 场景四 |
| `INTENT_ELEMENT_CHECK` | 要素合规审查 | 要素、矛盾、缺失、无效、不规范、核验 | 场景五 |
| `INTENT_HISTORY` | 流转历史溯源 | 流转、历史、版本、背书记录、溯源 | 场景六 |
| `INTENT_BATCH_RISK` | 批量风险聚合 | 批量、同一出票人、风险汇总、敞口、集中度 | 场景七 |
| `INTENT_PLEDGE` | 质押融资评估 | 质押、融资、抵押、质押率、担保 | 场景八 |
| `INTENT_UNKNOWN` | 未能识别 | —（其余全部兜底） | 模糊检索（见 P3） |

**路由数据流**：

```
用户输入 query
         ↓
┌─────────────────────────────────────────────┐
│  第一阶段：关键词快速通道（本地，无网络延迟）    │
│  遍历各意图的关键词列表，若命中 → 直接路由      │
│  confidence = 0.95（关键词命中置信度最高）      │
└─────────────────────────────────────────────┘
         ↓ 未命中
┌─────────────────────────────────────────────┐
│  第二阶段：qwen-max 语义意图识别               │
│  发送结构化 Prompt → 返回 intent_id + reason  │
│  + confidence（0.0 ~ 1.0）                   │
└─────────────────────────────────────────────┘
         ↓
  ├── confidence ≥ 0.75 且 intent_id ≠ UNKNOWN
  │     → 专项检索（调用对应场景处理器）
  └── 其余（UNKNOWN 或 confidence < 0.75）
        → 模糊检索（见 P3）
         ↓
记录日志：IntentLog（query, intent_id, confidence, classify_method, route_type）
  classify_method: "keyword" | "llm"  ← 记录是哪个阶段命中的，用于后续优化
```

**IntentRouter 实现策略（两阶段串联）**：

```python
# 阶段一：关键词快速通道（优先，零延迟）
def _keyword_classify(query: str) -> tuple[str, float] | None:
    for intent_id, keywords in INTENT_KEYWORDS.items():
        if any(kw in query for kw in keywords):
            return intent_id, 0.95  # 关键词命中，置信度固定 0.95
    return None  # 未命中，交给第二阶段

# 阶段二：qwen-max 语义意图识别（关键词未命中时调用）
async def _llm_classify(query: str) -> tuple[str, float]:
    prompt = f"""你是票据业务意图识别器。根据用户输入，从以下意图中选择最匹配的一个，
并给出 0~1 的置信度。若无法匹配任何意图，返回 INTENT_UNKNOWN。

可选意图列表：
- INTENT_PRICING：用户询问票据贴现利率、定价、报价等
- INTENT_ENDORSER_CHECK：用户询问背书链合规性、背书人资质、连续性等
- INTENT_EXPIRY：用户询问票据到期处理、追索权、时效计算等
- INTENT_AML：用户询问大额交易合规、反洗钱报告义务等
- INTENT_ELEMENT_CHECK：用户询问票据要素是否合规、有无矛盾或缺失等
- INTENT_HISTORY：用户询问票据流转历史、背书记录溯源等
- INTENT_BATCH_RISK：用户询问多张票据的风险汇总、承兑人集中度等
- INTENT_PLEDGE：用户询问票据质押融资方案、质押率等
- INTENT_UNKNOWN：无法归入以上任何类别

用户输入：「{query}」

严格按 JSON 格式返回，不要有任何额外说明：
{{"intent_id": "INTENT_XXX", "confidence": 0.85, "reason": "简短说明"}}"""

    response = await openai_client.chat.completions.create(
        model="qwen-max",                # 使用已配置的 qwen-max 模型
        messages=[{"role": "user", "content": prompt}],
        temperature=0.0,                 # 意图识别要求确定性，temperature=0
        max_tokens=100,                  # 只需返回简短 JSON，限制 token 避免浪费
        response_format={"type": "json_object"},  # 强制 JSON 输出
    )
    result = json.loads(response.choices[0].message.content)
    return result["intent_id"], result["confidence"]

# 主入口：两阶段串联
async def classify(query: str) -> tuple[str, float, str]:
    # 先尝试关键词通道
    kw_result = _keyword_classify(query)
    if kw_result:
        return kw_result[0], kw_result[1], "keyword"  # (intent_id, confidence, method)
    # 关键词未命中 → 调用 qwen-max
    intent_id, confidence = await _llm_classify(query)
    return intent_id, confidence, "llm"
```

**qwen-max 调用说明**：

- 复用 `settings.OPENAI_BASE_URL` + `settings.OPENAI_API_KEY` 已有配置（通义千问兼容 OpenAI 接口）
- 模型固定使用 `qwen-max`（`settings.OPENAI_MODEL` 用于 RAG 答案生成，意图识别单独指定避免相互影响）
- `temperature=0.0`：意图分类为确定性任务，不需要创造性，固定为 0
- `response_format={"type": "json_object"}`：强制结构化输出，避免解析失败
- 预期延迟：关键词通道 < 1ms；qwen-max 通道 200ms ~ 500ms（仅在关键词未命中时触发）

**为什么不能只用关键词**：用户实际输入往往是"这张票快到期了我该怎么处理"、"帮我看看这个背书有没有问题"等自然语言，不包含精确关键词，纯规则会将其全部误判为 `INTENT_UNKNOWN` 并降级到模糊检索，损失专项检索的精准度和响应质量。qwen-max 的语义理解能力可以覆盖这类口语化、上下文化的表达。

**为什么是前置需求**：P2 是所有专项场景的入口调度层；没有意图路由，场景一到场景八全部无法被正确触发，只能走无差别模糊检索。

---

### P3 检索质量评估 + 转人工兜底机制

**背景**：当前系统无法判断检索结果是否足以回答用户问题，只要有 top-k 结果就直接送入 LLM。需要增加质量评估层，在结果不足时主动告知用户并触发转人工。

**检索质量评估指标**：

```
RetrievalQuality:
  - top1_score:      最高相似度分数（Reranker 输出）
  - avg_score:       前 N 条的平均分数
  - hit_count:       有效命中数（分数 ≥ 阈值的条目数）
  - coverage_ratio:  命中内容覆盖用户问题关键词的比例

判断规则：
  质量=SUFFICIENT  ← top1_score ≥ 0.75 且 hit_count ≥ 2
  质量=PARTIAL     ← top1_score ≥ 0.55 且 hit_count ≥ 1（给出答案但附加免责提示）
  质量=INSUFFICIENT← top1_score < 0.55 或 hit_count = 0 → 触发转人工
```

**转人工响应模板**：

```
{
  "answer": "抱歉，当前知识库中暂无足够的内容来准确回答您的问题。
             建议您联系专业票据顾问获得进一步支持。",
  "transfer_to_human": true,
  "contact_info": {
    "hotline": "400-XXX-XXXX",
    "online_service": "工作日 9:00-18:00",
    "ticket_id": "<自动生成工单号>"
  },
  "query_saved": true   // 告知用户此问题已记录，将用于知识库优化
}
```

**为什么是前置需求**：没有质量评估，LLM 会基于低质量检索结果"编造"答案（幻觉），在票据合规场景下后果严重；转人工兜底是金融类产品的基本安全要求。

---

### P4 查询行为后台日志存储（优化闭环）

**背景**：现有 `QueryLog` 表记录了问答内容，但未区分检索类型（专项/模糊/未命中），无法支撑专项检索的迭代优化。需新增两张表。

**需新增数据表**：

```
IntentLog（意图识别日志）
  - id:            UUID
  - tenant_id:     所属租户
  - user_id:       操作用户
  - query:         原始用户输入
  - intent_id:     识别到的意图（INTENT_PRICING / INTENT_UNKNOWN 等）
  - confidence:    置信度（0.0 ~ 1.0）
  - route_type:    路由类型（specialized / fuzzy / no_result）
  - created_at:    时间戳

SearchMissLog（检索未命中日志）
  - id:            UUID
  - tenant_id:     所属租户
  - query:         原始用户输入
  - intent_id:     意图 ID
  - route_type:    fuzzy / specialized（区分是哪种检索未命中）
  - top1_score:    最高检索分（即使未命中也记录分数）
  - retrieval_ms:  检索耗时
  - transferred:   是否触发了转人工（Boolean）
  - created_at:    时间戳
```

**三类存储分类规则**：

```
专项检索命中     → QueryLog（route_type="specialized"）+ IntentLog
专项检索未命中   → SearchMissLog（route_type="specialized"）+ IntentLog
模糊检索命中     → QueryLog（route_type="fuzzy"）+ IntentLog
模糊检索未命中   → SearchMissLog（route_type="fuzzy"）+ IntentLog + 转人工
```

**后台分析用途**：

- 按 `intent_id` 聚合 `SearchMissLog`，高频未命中意图 → 优先扩充对应专项知识库
- 按 `confidence` 分布分析 `IntentLog` → 调整意图分类阈值
- 按 `route_type` 分布分析 → 评估专项检索覆盖率，指导新增专项场景

---

## 二、交互流程设计

---

### F1 用户无输入时的友情引导

**触发条件**：用户发送空消息、纯空格、或仅发送文件但未附带文字说明。

**数据流**：

```
用户发送空 query（len(query.strip()) == 0）
         ↓
系统返回友情提示（不进入检索流程）：

"您好！我是票据业务智能顾问，可以帮您处理以下方面的问题：

 📋 票据要素查询    — 票据号码、金额、出票人、到期日等信息核查
 📊 贴现定价参考    — 根据承兑人和期限查询当前市场贴现利率区间
 🔗 背书链合规审查  — 背书人连续性、空白背书、签章规范核验
 ⏰ 到期日追索咨询  — 追索权时效计算、拒付处理流程
 🏦 大额交易合规    — 反洗钱报告义务、客户尽职调查要求
 📜 监管法规解读    — 票据法、人行规章、银保监规定条款查询
 🔍 票据风险评估    — 承兑人风险、背书人黑名单、要素矛盾检测

请问您需要了解哪方面的内容？
（也可以直接上传票据图片，我会自动识别票面信息并为您提供分析）"
```

**实现要点**：
- 返回结构体中 `answer_type = "guide_prompt"`，前端可渲染为可点击的快捷入口按钮
- 快捷按钮点击后自动填充对应的 `query` 模板（如"请帮我查询票据贴现利率"）
- 不写入 `QueryLog`，不写入 `IntentLog`（引导页不算有效查询）

---

### F2 完整问答主链路（含所有分支）

**数据流**：

```
用户输入 query（非空）
         ↓
┌─────────────────────────────────────────────────┐
│  IntentRouter.classify(query)                   │
│  → intent_id, confidence                        │
└─────────────────────────────────────────────────┘
         ↓
  ┌──────────────────────────────────────────────────────────────┐
  │ confidence ≥ 0.75 且 intent_id ≠ UNKNOWN                    │
  │ → 专项检索处理器（对应场景一～八）                              │
  │   · 以 BillElement 字段构造专项 query                         │
  │   · 调用 VectorStore.hybrid_search()                         │
  │   · 评估 RetrievalQuality                                    │
  │     ├── SUFFICIENT/PARTIAL → LLM 生成答案 → 返回用户          │
  │     │   写入 QueryLog（route_type="specialized"）             │
  │     │   写入 IntentLog                                       │
  │     └── INSUFFICIENT                                         │
  │           写入 SearchMissLog（route_type="specialized"）      │
  │           写入 IntentLog                                     │
  │           → 触发转人工（见 P3）                               │
  └──────────────────────────────────────────────────────────────┘
         ↓ （confidence < 0.75 或 UNKNOWN）
  ┌──────────────────────────────────────────────────────────────┐
  │ 模糊检索：VectorStore.hybrid_search(query_raw)               │
  │   · 不构造专项 query，直接用原始问题向量检索                    │
  │   · 评估 RetrievalQuality                                    │
  │     ├── SUFFICIENT/PARTIAL → LLM 生成答案 → 返回用户          │
  │     │   写入 QueryLog（route_type="fuzzy"）                   │
  │     │   写入 IntentLog                                       │
  │     └── INSUFFICIENT                                         │
  │           写入 SearchMissLog（route_type="fuzzy"）            │
  │           写入 IntentLog                                     │
  │           → 触发转人工（见 P3）                               │
  └──────────────────────────────────────────────────────────────┘
```

---

## 三、专项检索场景（共 8 个）

---

### 场景一：承兑人资信联查 × 贴现定价辅助

**意图 ID**：`INTENT_PRICING`

**触发字段**：`acceptor`（承兑人）、`ticket_type`（票据类型）、`due_date`（到期日）、`amount_numeric`（金额）

**数据流**：

```
识别到 acceptor = "杭州银行股份有限公司"
        ticket_type = "银行承兑汇票"
        due_date = "2025-03-15"（距今约 6 个月）
        amount_numeric = 10_000_000
         ↓
构造专项 RAG 查询（2条并行）：
  Q1 = "杭州银行 承兑额度 授信评级 风险等级"
  Q2 = "银行承兑汇票 6个月期 贴现利率 定价基准 市场行情"
         ↓
hybrid_search(Q1) → 命中：内部授信政策文件"A类城商行定价基准第3.2条"
hybrid_search(Q2) → 命中：最新市场利率区间文档"2024Q4转贴现利率周报"
         ↓
LLM 结合到期天数（系统自动计算）和 amount 分档：
         ↓
输出：
  "承兑人：杭州银行（内部评级 AA 类城商行）
   期限：约 182 天（6个月）
   建议贴现利率区间：2.10% - 2.35%
   参考依据：[内部授信政策 § 3.2] + [2024Q4利率周报]
   注：1000万以上大额贴现需分支行授权，请联系业务负责人"
```

**为什么紧密**：单纯问"利率是多少"RAG 无法返回精确结果；必须将识别到的承兑人主体、期限（由到期日实时计算）、金额档位三个字段联合构造查询，才能匹配到对应分档的定价文件段落。

---

### 场景二：背书链穿透 × 失信名单核查

**意图 ID**：`INTENT_ENDORSER_CHECK`

**触发字段**：`endorsers`（背书人列表）、`drawer`（出票人）、`payee`（初始收款人）

**数据流**：

```
识别到 endorsers = ["甲贸易有限公司", "乙物流集团", "丙实业"]
        drawer = "丁科技有限公司"
         ↓
对每个主体并行构造查询（共4条）：
  Q_甲 = "甲贸易有限公司 失信被执行 票据拒付 风险记录"
  Q_乙 = "乙物流集团 黑名单 制裁 经营异常"
  Q_丙 = "丙实业 背书人风险 逾期"
  Q_丁 = "丁科技有限公司 出票人 信用 违约"
         ↓
hybrid_search × 4（并行执行）
         ↓
LLM 汇总，逐主体输出风险评估：
  "背书链核查结果（共4个主体）：
   ✅ 甲贸易有限公司 — 知识库未发现风险记录
   ⚠️ 乙物流集团 — 命中 [票据拒付历史.xlsx 第47行]：
      2023-09 曾有商票逾期，金额 380 万元，建议追加审核
   ✅ 丙实业 — 知识库未发现风险记录
   ✅ 丁科技有限公司（出票人）— 知识库未发现风险记录
   综合建议：该票背书链存在1处历史风险，建议要求乙物流集团
            提供近6个月银行流水或补充信用担保"
```

**为什么紧密**：背书人名单是视觉识别后才有的动态数据，RAG 知识库里的风险文档无法主动关联；必须以每个背书人名称为索引键分别检索，才能实现名单穿透核查。

---

### 场景三：到期日自动触发追索权时效核查

**意图 ID**：`INTENT_EXPIRY`

**触发字段**：`due_date`（到期日）、`ticket_type`（票据类型）、`issue_date`（出票日）

**数据流**：

```
识别到 due_date = "2024-03-15"
        ticket_type = "银行承兑汇票"
        （系统当前日期：2026-05-18）
         ↓
系统计算：逾期天数 = 792 天
         ↓
构造专项查询：
  Q = "银行承兑汇票 持票人追索权 诉讼时效 到期后多少天 票据法第17条"
         ↓
hybrid_search(Q) → 命中：
  [票据法第17条]：持票人对出票人、承兑人的权利，自票据到期日起2年
  [最高法司法解释第19条]：追索权自被拒绝承兑或拒绝付款日起6个月
         ↓
LLM 结合 792 天逾期计算结论：
  "票据状态：已逾期 792 天
   法定时效判断：
   · 对承兑人追索权：票据法规定 2 年，已超期（792天 > 730天），
     持票人丧失票据权利，无法向承兑人主张付款
   · 对背书人追索权：6 个月内，已超期
   当前建议：仅能依据出票人与持票人之间的基础合同关系
             主张民事权利，建议立即咨询法律顾问评估诉讼可行性"
```

**为什么紧密**：法律时效是知识库里的静态规则，但"是否超时"必须用识别出的到期日配合系统当前日期动态计算后再与法规比对；单纯检索无法得出"已超期/未超期"的结论。

---

### 场景四：大额票据合规义务自动触发

**意图 ID**：`INTENT_AML`

**触发字段**：`amount_numeric`（金额）、`drawer`（出票人）、`drawer_bank`（出票人开户行）、`ticket_type`（票据类型）

**数据流**：

```
识别到 amount_numeric = 5_200_000（520万）
        drawer = "XX贸易有限公司"
        drawer_bank = "中国工商银行杭州西湖支行"
         ↓
规则前置（无需 RAG，直接判断）：
  金额 ≥ 500 万 → 触发大额交易合规检查标记
         ↓
构造专项查询（2条并行）：
  Q1 = "大额交易报告 500万 票据业务 反洗钱 义务 时限"
  Q2 = "贴现业务 客户尽职调查 出票人 开户行核验 大额"
         ↓
hybrid_search × 2 → 命中：
  [反洗钱管理办法第5条]：单笔交易金额 ≥ 500万 须报送大额报告
  [内部合规手册第3章]：贴现前须完成出票人工商信息、账户信息核验
         ↓
LLM 输出合规义务清单：
  "合规检查触发（票面金额 520 万元，超过 500 万报告门槛）：
   ☑ 需在交易发生后 5 个工作日内向人民银行报送大额交易报告
     依据：《金融机构大额交易和可疑交易报告管理办法》第5条
   ☑ 须核验出票人 [XX贸易有限公司] 工商注册信息与账户一致性
     依据：[内部合规手册 § 3.1]
   ☑ 须存档出票人营业执照、法人身份证复印件
   ⚠ 如出票人为自然人，需额外进行受益所有人核查"
```

**为什么紧密**：合规触发阈值（50万/500万/1000万）是规则判断，规则命中后再通过 RAG 定位到具体的监管条款和内部手册条目；金额字段由识别提供，无识别则无法触发。

---

### 场景五：票据要素矛盾检测 × 监管条文定位

**意图 ID**：`INTENT_ELEMENT_CHECK`

**触发字段**：`BillElement` 全字段组合（大小写金额、出票日、到期日、票据类型、承兑人性质）

**数据流**：

```
识别结果：
  amount_numeric = 1_000_000
  amount_text    = "壹佰万元整"  ← ✅ 一致（100万 = 壹佰万）
  issue_date     = "2024-09-15"
  due_date       = "2024-01-20"  ← ❌ 到期日早于出票日
  ticket_type    = "银行承兑汇票"
  acceptor       = "XX贸易有限公司"  ← ❌ 银票承兑人为非银行主体
         ↓
识别层检测到 2 处结构性矛盾 → 生成矛盾描述注入查询：
  Q1 = "票据到期日早于出票日 法律效力 如何认定 票据法必要记载事项"
  Q2 = "银行承兑汇票 承兑人 必须是银行 商业主体承兑是否有效 票据法第38条"
         ↓
hybrid_search × 2 → 命中：
  [票据法第22条]：汇票必须记载的事项——到期日不得早于出票日
  [票据法第38条]：银行承兑汇票承兑人须为银行业金融机构
         ↓
LLM 输出结构化矛盾报告：
  "发现 2 处合规风险（建议拒绝收票）：

   [风险1 — 严重] 日期逻辑错误
   到期日 2024-01-20 早于出票日 2024-09-15
   依据：《票据法》第22条——汇票到期日不得早于出票日
   结论：该票据欠缺绝对必要记载事项，票据无效

   [风险2 — 严重] 承兑人资格不符
   银行承兑汇票承兑人为商业主体「XX贸易有限公司」
   依据：《票据法》第38条、《商业汇票承兑贴现与再贴现管理办法》第6条
   结论：承兑人资格不符，该票名为银票实为商票，
         请重新核实票据类型或要求出票人更正"
```

**为什么紧密**：矛盾由多字段交叉计算发现（只有识别出全部字段才能比较），RAG 定位对应法规条款，LLM 输出"识别证据 + 法规依据"双重支撑的报告；这是人工审票流程的完整数字化复现。

---

### 场景六：票据流转历史溯源与版本对比

**意图 ID**：`INTENT_HISTORY`

**触发字段**：`ticket_number`（票据号码）+ `BillVersion` 表历史记录

**数据流**：

```
用户上传同一张票据的第3次流转图片
         ↓
识别 → ticket_number = "EA2024001234"
         ↓
查询 BillVersion：发现历史版本 v1（2024-09-20）、v2（2024-11-05）
         ↓
对比 v2 → v3 新增背书人：["戊金融服务有限公司"]
         ↓
构造 RAG 查询：
  Q1 = "戊金融服务有限公司 风险 黑名单 票据纠纷"（新增背书人核查）
  Q2 = "票据背书转让 3次流转 背书连续性 规范要求"（流转合规性检查）
         ↓
hybrid_search × 2 → 命中：
  [内部风险名单.xlsx]：未发现戊金融服务有限公司记录
  [票据法第31条]：背书连续性要求——被背书人与背书人名称须一致
         ↓
LLM 输出流转溯源报告：
  "票据号码 EA2024001234 流转历史：

   第1次流转（2024-09-20）：
     收款人 YY科技 → 背书给 丙实业
   第2次流转（2024-11-05）：
     丙实业 → 背书给 丁物流
   第3次流转（本次，2025-01-10）：
     丁物流 → 背书给 戊金融服务有限公司 ✅ 新增
   
   背书连续性检查：✅ 三次背书连续，无断链
   新增背书人核查：✅ 知识库未发现风险记录
   当前持票人：戊金融服务有限公司"
```

**为什么紧密**：流转历史来自 `BillVersion` 数据库，背书合规规则来自 RAG 法规库，新增背书人风险来自 RAG 风险名单库；三个数据源缺一不可，均由票据号码这一识别字段串联。

---

### 场景七：跨票据批量风险聚合（同承兑人 / 同出票人）

**意图 ID**：`INTENT_BATCH_RISK`

**触发字段**：`acceptor`（承兑人）或 `drawer`（出票人）+ `BillRecord` 表同主体批量查询

**数据流**：

```
用户上传一批票据（10张），识别后：
  全部 acceptor = "某地方城商行"
  金额汇总：8500 万元
         ↓
查询 BillRecord：该租户下同一承兑人的所有票据
  → 历史持有：12 张，合计 3.2 亿元
  → 加上本批：合计 3.285 亿元
         ↓
构造 RAG 查询：
  Q1 = "某地方城商行 承兑额度上限 集中度风险 内部限额"
  Q2 = "单一承兑行 承兑敞口 风险集中 银保监规定"
         ↓
hybrid_search × 2 → 命中：
  [内部授信政策第5.3条]：单一城商行承兑敞口不超过 2 亿元
  [银保监指导意见]：票据业务应分散承兑行，防止集中度风险
         ↓
LLM 输出批量风险报告：
  "承兑人集中度风险预警：

   承兑人：某地方城商行
   当前累计敞口：人民币 3.285 亿元
   内部限额：2 亿元
   超限金额：1.285 亿元（超限 64.3%）⚠️
   
   依据：[内部授信政策 § 5.3]
   建议：暂停受理该行承兑票据，待敞口降至限额以下后恢复，
         或申请风险委员会审批提额"
```

**为什么紧密**：单张票据看不出集中度问题，必须通过 `BillRecord` 聚合同一承兑人的所有持仓，再用 RAG 检索内部限额政策，两者结合才能触发"超限预警"。

---

### 场景八：票据质押融资可行性评估

**意图 ID**：`INTENT_PLEDGE`

**触发字段**：`ticket_type`（票据类型）、`acceptor`（承兑人）、`due_date`（到期日）、`amount_numeric`（金额）、`endorsers`（背书链完整性）

**数据流**：

```
识别到：
  ticket_type    = "银行承兑汇票"
  acceptor       = "招商银行"
  due_date       = "2025-06-30"（距今约 7 个月）
  amount_numeric = 5_000_000
  endorsers      = ["甲公司", "乙公司"]（背书链完整）
         ↓
构造专项查询（3条并行）：
  Q1 = "银行承兑汇票质押 招商银行承兑 质押率 折扣率"
  Q2 = "票据质押融资 7个月期 利率 质押贷款条件"
  Q3 = "票据质押 背书要求 质押背书 格式规范"
         ↓
hybrid_search × 3 → 命中：
  [内部质押业务手册]：大行承兑银票质押率通常 90%-95%
  [市场利率参考]：当前质押融资利率参考区间 2.8%-3.2%
  [票据法第35条]：质押背书须注明"质押"字样
         ↓
LLM 输出质押评估报告：
  "质押可行性评估：

   票据基本信息：招商银行承兑银票，500万，剩余期限 ~7 个月
   
   质押融资方案（参考）：
   · 质押率：90%（大行承兑银票标准质押率）
   · 可融资金额：450 万元
   · 参考利率：2.8% - 3.2%（7个月期）
   · 融资成本（月）：约 1.17 万 - 1.33 万元
   
   背书要求：
   · 需在票据背面添加质押背书，注明「质押」字样
   · 依据：《票据法》第35条
   
   注意事项：
   · 当前背书链有 2 手，需确认最终背书人有权质押
   · 质押期间票据由受质方保管，到期前须解质或展期"
```

**为什么紧密**：质押率依赖承兑人主体（大行/城商行/商业主体），融资期限依赖到期日计算，可融金额依赖票面金额；这三个字段均来自识别，RAG 提供政策依据，LLM 综合输出完整方案，缺少任何一个环节都无法生成有价值的评估报告。

---

## 四、字段→场景覆盖矩阵

| BillElement 字段 | 场景一 | 场景二 | 场景三 | 场景四 | 场景五 | 场景六 | 场景七 | 场景八 |
|---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| ticket_number | | | | | | ✅ | | |
| ticket_type | ✅ | | ✅ | ✅ | ✅ | | | ✅ |
| issue_date | | | ✅ | | ✅ | | | |
| due_date | ✅ | | ✅ | | ✅ | | | ✅ |
| amount_numeric | ✅ | | | ✅ | ✅ | | ✅ | ✅ |
| amount_text | | | | | ✅ | | | |
| drawer | | ✅ | | ✅ | | | ✅ | |
| drawer_bank | | | | ✅ | | | | |
| acceptor | ✅ | | | | ✅ | | ✅ | ✅ |
| payee | | ✅ | | | | ✅ | | |
| endorsers | | ✅ | | | | ✅ | | ✅ |
| drawee_bank | | | | | | | | |

---

## 五、优先级与实现顺序

| 优先级 | 需求 | 理由 |
|---|---|---|
| P0（必须先做） | P1 票据生命周期管理 | 所有场景依赖此数据结构 |
| P0（必须先做） | P2 意图识别路由 | 问答主链路的核心调度层 |
| P0（必须先做） | P3 检索质量评估 + 转人工 | 金融场景的安全底线 |
| P1（核心功能） | F1 友情引导 | 提升用户体验，降低空查询率 |
| P1（核心功能） | 场景一、五（定价 + 要素检测） | 最高频业务场景 |
| P1（核心功能） | P4 查询日志存储 | 支撑持续优化 |
| P2（重要功能） | 场景二、三、四（背书/追索/合规） | 合规审查核心场景 |
| P3（完善功能） | 场景六、七、八（溯源/批量/质押） | 高价值进阶场景 |
