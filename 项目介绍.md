# 票据合规智能审核平台

> 面向票据金融机构的多租户 AI 知识库 + 多智能体合规审核平台——支持票据法规文档智能问答、票据要素结构化识别、票据流转生命周期管理、多 Agent 协作全流程合规审核，融合检索增强生成（RAG）、视觉大模型与多智能体编排能力。

---

## 目录

1. [项目概览](#1-项目概览)
2. [技术架构](#2-技术架构)
3. [功能模块详解（RAG 基础层）](#3-功能模块详解rag-基础层)
   - 3.1 [用户认证与多租户 RBAC](#31-用户认证与多租户-rbac)
   - 3.2 [文档入库流水线](#32-文档入库流水线)
   - 3.3 [三路混合检索引擎](#33-三路混合检索引擎)
   - 3.4 [智能问答（RAG）](#34-智能问答rag)
   - 3.5 [流式问答](#35-流式问答)
   - 3.6 [票据要素识别](#36-票据要素识别)
   - 3.7 [票据生命周期管理](#37-票据生命周期管理)
   - 3.8 [意图路由与专项检索](#38-意图路由与专项检索)
   - 3.9 [检索质量评估与转人工](#39-检索质量评估与转人工)
   - 3.10 [租户管理](#310-租户管理)
   - 3.11 [系统监控](#311-系统监控)
   - 3.12 [前端管理后台](#312-前端管理后台)
   - 3.13 [多层次业务缓存](#313-多层次业务缓存appcorecopepy)
4. [多智能体合规审核系统（Agent 层）](#4-多智能体合规审核系统agent-层)
   - 4.1 [Agent 系统架构](#41-agent-系统架构)
   - 4.2 [BaseAgent 基础框架](#42-baseagent-基础框架)
   - 4.3 [OrchestratorAgent（任务编排）](#43-orchestratoragent任务编排)
   - 4.4 [M3 解析与识别层](#44-m3-解析与识别层)
   - 4.5 [M4 合规检索层](#45-m4-合规检索层)
   - 4.6 [M5 风险评估层](#46-m5-风险评估层)
   - 4.7 [M6 报告生成层](#47-m6-报告生成层)
   - 4.8 [M7 流转追踪层](#48-m7-流转追踪层)
   - 4.9 [M8 欺诈检测层](#49-m8-欺诈检测层)
   - 4.10 [M9 批量调度层](#410-m9-批量调度层)
   - 4.11 [M10 出票预检](#411-m10-出票预检)
5. [数据库设计](#5-数据库设计)
   - 5.1 [原有 PostgreSQL 表（RAG 基础层）](#51-原有-postgresql-表rag-基础层)
   - 5.2 [新增 14 张表（Agent 层）](#52-新增-14-张表agent-层)
   - 5.3 [Milvus 向量集合](#53-milvus-向量集合)
6. [API 接口总览](#6-api-接口总览)
7. [测试覆盖](#7-测试覆盖)
8. [部署架构](#8-部署架构)
9. [目录结构](#9-目录结构)

---

## 1. 项目概览

### 背景

票据业务（银行承兑汇票、商业承兑汇票等）涉及大量法规文件、操作规程和业务规则，从业人员需频繁查阅规章制度，同时需要对每张票据进行多维度合规审核。传统方式依赖人工检索和人工审核，效率低且一致性差，容易遗漏关键合规要素。

### 完整业务流程

```
业务员打开 RAG 聊天窗口
      ↓
上传票据图片 + 输入问题（如"这张票据能做贴现吗？"）
      ↓
系统自动识别票据要素（视觉大模型 + Redis 缓存）
      ↓
意图识别路由
    ├── RAG 知识意图  →  检索法规知识库 → 返回知识性答案
    └── Agent 审核意图 →  调用对应 Agent 组合 → 返回结构化审核报告
      ↓
业务员拿到报告（"可执行 / 不可执行 / 需关注"）
      ↓
决定是否在业务系统发起实际业务流程
      ↓
业务系统调用专项校验接口（/api/v1/audit/tasks）做二次确认
```

**Agent 意图 → 内部路由映射（业务系统无需感知）：**

| 用户问题示例 | 识别意图 | 内部 task_type | 执行 Agent 链 |
|------------|--------|--------------|-------------|
| 这张票据能否出票 | INTENT_AGENT_ISSUANCE | issuance_check | 解析→要素→合规→出票预检→风险 |
| 能否做贴现 | INTENT_AGENT_DISCOUNT | discount_apply | 解析→要素→合规+背书+欺诈→合同→风险 |
| 帮我审核背书链 | INTENT_AGENT_ENDORSEMENT | endorsement | 解析→要素→背书链→欺诈→风险 |
| 欺诈筛查 | INTENT_AGENT_FRAUD | full_audit | 全流程（含欺诈） |
| 合规审核报告 | INTENT_AGENT_COMPLIANCE | issuance_check | 合规重点路径 |
| 全流程审核 | INTENT_AGENT_FULL_AUDIT | full_audit | 所有 12 个 Agent |

本系统分为两层：
- **RAG 基础层**：通过检索增强生成技术将非结构化法规文档转化为可检索的知识库，结合视觉大模型实现票据要素结构化提取
- **多智能体 Agent 层**：12 个专项 Agent 分工协作，对票据进行全流程自动化合规审核，覆盖要素提取、合规检索、背书链验证、合同审核、风险评分、欺诈检测等全链路

### 核心能力

| 能力 | 描述 |
|------|------|
| **票据咨询（新）** | 聊天窗口一站式：上传票据图片 + 提问 → 意图路由 → 返回审核报告或知识答案 |
| **智能问答** | 基于票据法规文档库，精准回答业务问题，附带原文引用 |
| **增强问答** | 意图路由 + 质量评估 + 票据上下文注入 + 转人工兜底 |
| **票据识别** | 上传票据图片/PDF，自动提取 18 个结构化要素 |
| **全流程审核** | 12 个 Agent 协作，从文档解析到风险报告全自动化 |
| **合规检索** | 18 个要素字段并行 RAG 查询，每字段对应法规条文 |
| **欺诈检测** | 五维并行：印章真伪+重复票据+图像篡改+关联网络+黑名单 |
| **背书链验证** | 有向图重建 + 9 类违规检测（断链/闭环/重复/空白等）|
| **风险评分** | 四维加权评分（合规30%+背书30%+合同20%+欺诈20%） |
| **PDF 报告** | reportlab 渲染 A4 合规审核报告，支持培训注解模式 |
| **流转追踪** | 七步报文状态机，实时追踪票据在跨行系统中的流转 |
| **批量审核** | asyncio.Semaphore 控制并发，支持百张票据批量处理 |
| **多租户隔离** | 不同机构数据完全隔离，支持独立配额和限流策略 |
| **混合检索** | 语义向量 + 关键词权重 + BM25 三路融合 |
| **多层次缓存** | 票据指纹（18字段SHA-256）+ 五层Redis缓存，首次全跑后重复请求 < 100ms |

### 技术栈总览

```
后端框架   FastAPI 0.111 + Python 3.11（异步 async/await）
MCP 工具层  FastMCP（mcp[cli]>=1.0.0）— 11 个 Agent 能力封装为 MCP 工具，挂载 /mcp
执行引擎   LangGraph（替代自研 DAGExecutor）— PostgresSaver 状态外置，支持多容器
多智能体   自研 BaseAgent + 11 个专项 Agent（业务逻辑层，不感知 LangGraph/MCP）
向量数据库  Milvus 2.4（三路混合检索 + 分区隔离）
关系数据库  PostgreSQL 16（业务数据 + LangGraph 检查点，共 25+ 张表）
缓存/限流  Redis 7（QPS限流 + 五层业务缓存，TTL按层次从 2h 到 24h）
嵌入模型   BGE-M3（1024 维稠密向量 + 稀疏向量）
精排模型   BGE-Reranker-v2-m3
视觉模型   Qwen-VL / Qwen-VL-Max（票据要素识别）
LLM        OpenAI API 兼容（gpt-4o 等）/ Anthropic Claude
OCR        PaddleOCR 2.7（中文印刷体）
PDF 解析   Camelot + pdfplumber + PyMuPDF（三层降级）
PDF 渲染   reportlab 4.2.2（合规审核报告生成）
可观测性   ContextVar Trace ID + Prometheus + Grafana（多容器日志聚合）
前端       原生 HTML/CSS/JavaScript（无框架依赖）
容器化     Docker Compose（单机） / K8s 无状态多副本（生产）
```

---

## 2. 技术架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端（Browser）                           │
│  登录页  │  聊天问答  │  管理后台（文档/租户/分析/监控）             │
└──────────────────────────┬──────────────────────────────────────┘
                           │ HTTP / SSE
┌──────────────────────────▼──────────────────────────────────────┐
│              FastAPI 应用层（Port 8000，无状态可多副本）            │
│  JWT 中间件 │ Redis 限流 │ GZip │ Trace-ID 中间件（ContextVar）  │
│                                                                   │
│  ── RAG 基础层路由 ─────────────────────────────────────────────  │
│  /auth  /documents  /query  /tenants  /bills                     │
│                                                                   │
│  ── Agent 层路由 ───────────────────────────────────────────────  │
│  /audit（审核任务）  /batch（批量调度）  /tracking（流转追踪）       │
│                                                                   │
│  ── MCP 端点 ──────────────────────────────────────────────────  │
│  POST /mcp   → MCP Streamable HTTP（外部 LLM 调用工具）           │
│  GET  /mcp/sse → SSE 事件流                                       │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │               MCP 工具层（app/mcp/）                        │  │
│  │  FastMCP 实例（11 个工具） ← Claude API / Claude Desktop    │  │
│  │  parse_bill_document │ extract_bill_elements               │  │
│  │  check_compliance    │ analyze_endorsement_chain           │  │
│  │  detect_fraud        │ review_contract │ assess_risk       │  │
│  │  generate_report     │ check_bill_issuance                 │  │
│  │  track_bill_flow     │ schedule_batch_audit                │  │
│  └────────────────┬───────────────────────────────────────────┘  │
│                   │ 进程内直接调用（无 HTTP 开销）                 │
│  ┌────────────────▼───────────────────────────────────────────┐  │
│  │           LangGraph 执行引擎（app/graph/）                   │  │
│  │  OrchestratorAgent.run() → build_audit_graph(task_type)   │  │
│  │  BillAuditState（TypedDict）← → PostgresSaver（外置状态）   │  │
│  │  graph.astream() 流式执行，每节点完成更新 DB 进度            │  │
│  │  条件边：关键节点失败→END，并行层：asyncio.gather 并发       │  │
│  └────────────────┬───────────────────────────────────────────┘  │
│                   │ 调用 Agent.run()                              │
│  ┌────────────────▼───────────────────────────────────────────┐  │
│  │              专项 Agent 业务逻辑层（app/agents/）            │  │
│  │  DocumentParser │ ElementExtraction │ ComplianceRetrieval  │  │
│  │  EndorsementChain │ FraudDetection  │ ContractReview       │  │
│  │  RiskAssessment   │ ReportGeneration│ FlowTracking         │  │
│  │  BatchScheduling  │ BillIssuance                           │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────┬─────────────────────┬────────────────────────┬────────────┘
       │                     │                        │
┌──────▼──────┐   ┌──────────▼──────────┐  ┌─────────▼──────────┐
│ PostgreSQL  │   │  Milvus 向量库       │  │  Redis             │
│ 业务 + LG   │   │  bill_documents 集合  │  │  限流 + 业务缓存    │
│ 检查点持久化 │   │  11 个字段           │  │  （5 层 TTL 策略）  │
└─────────────┘   └─────────────────────┘  └────────────────────┘

外部 LLM 调用路径（与主流程共用同一套 MCP 工具定义）：
  Claude API / Claude Desktop → HTTP → POST /mcp → FastMCP → Agent.run()
```

### Agent 数据流（全流程审核，LangGraph StateGraph 执行）

```
提交审核任务（文档 ID + 任务类型）
    │
    ▼
OrchestratorAgent.run()
    build_audit_graph(task_type, checkpointer=PostgresSaver)
    graph.astream(initial_state, config={"thread_id": audit_task_id})
    │
    ▼ LangGraph 节点 "parse"（调用 MCP 工具 parse_bill_document）
┌──────────────────┐
│ DocumentParser   │  解析 PDF/图片 → 获取文本
│ MCP 工具 / Agent │  → BillAuditState["parsed_doc"]（写入 PostgreSQL）
└────────┬─────────┘ 失败 → 条件边 END（after_parse）
         │
         ▼ 节点 "extract"（调用 MCP 工具 extract_bill_elements）
┌──────────────────┐
│ ElementExtraction│  识别 18 个票据要素
│ MCP 工具 / Agent │  → BillAuditState["bill_element"]（写入 PostgreSQL）
└────────┬─────────┘ 失败 → 条件边 END（after_extract）
         │
         ▼ 节点 "parallel_cef"（asyncio.gather 并发 3 个 MCP 工具）
┌────────────────────────────────────┐
│ check_compliance（MCP）            │  18 字段 × 并行 RAG 查询
│ analyze_endorsement_chain（MCP）   │  背书链有向图 + 9 类违规
│ detect_fraud（MCP）                │  五维欺诈评分
└─────────────────┬──────────────────┘
                  │ 关键节点任一失败 → END（make_after_parallel）
         ┌────────┴────────┐
         ▼                 ▼
节点 "contract"         节点 "risk"
review_contract（MCP）  assess_risk（MCP）
合同 6 项比对           四维加权评分 → 五档风险等级
         └────────┬────────┘
                  ▼ 节点 "report"
         generate_report（MCP）
         构建 9 节 JSON + 渲染 PDF → 写入 PostgreSQL
                  │
                  ▼ graph END
    final_state = await graph.aget_state(config)  # 从 PostgreSQL 读回
    _sync_state_to_context(final_state, ctx)      # 向后兼容 AgentContext
```

**LangGraph 状态持久化**：每个 LangGraph 节点执行后，`BillAuditState` 增量写入 PostgreSQL（LangGraph 检查点），`thread_id=audit_task_id`。容器重启后，任意副本均可通过 `graph.aget_state(config)` 恢复执行状态，实现真正的无状态容器。

---

## 3. 功能模块详解（RAG 基础层）

### 3.1 用户认证与多租户 RBAC

基于 JWT 的无状态身份认证，三级权限体系：`super_admin` → `tenant_admin` → `user`。

| 角色 | 可访问范围 | 典型操作 |
|------|-----------|---------|
| `super_admin` | 全系统所有租户 | 创建租户、查看所有数据 |
| `tenant_admin` | 本租户所有数据 | 上传文档、管理用户、查看统计 |
| `user` | 本租户 + 本人数据 | 问答聊天、上传票据、触发审核 |

**涉及核心文件**：[app/core/auth.py](app/core/auth.py)、[app/api/routers.py](app/api/routers.py)

---

### 3.2 文档入库流水线

支持 PDF / Word / Excel / 图片多格式，后台异步处理，MD5 去重，失败重试。

**流程**：文件保存 → PDF 三层降级解析 → 语义分块（800 token）→ BGE-M3 三路向量化 → 写入 Milvus → 写入 PostgreSQL

**PDF 解析降级策略**：Camelot lattice → Camelot stream → pdfplumber → PaddleOCR → Qwen-VL 兜底

**批量入库方式**：
- 单文件上传（`/upload`）：立即返回，后台异步解析
- 多文件批量上传（`/upload/batch`）：同批任一失败级联取消整批，可通过 `/batch/{id}/retry` 重试
- 文件夹批量入库（`/upload/folder`）：扫描服务器本地目录，`recursive=true` 递归扫描子目录

**涉及核心文件**：[app/services/ingestion.py](app/services/ingestion.py)、[app/services/pdf_parser.py](app/services/pdf_parser.py)

---

### 3.3 三路混合检索引擎

```
查询文本
    ├─ 稠密检索：BGE-M3 1024维向量 + Milvus HNSW → Top-20
    ├─ 稀疏检索：BGE-M3 SPLADE 稀疏向量 → Top-20
    └─ BM25 检索：jieba 分词 + BM25 词频倒排 → Top-20
    │
    ▼
RRF 融合（score = Σ 1/(k+rank_i)，k=60）→ Top-20
    │
    ▼
BGE-Reranker-v2-m3 精排 → 最终 Top-5
```

**涉及核心文件**：[app/services/vector_store.py](app/services/vector_store.py)、[app/services/embedding.py](app/services/embedding.py)

---

### 3.4 智能问答（RAG）

**标准问答**（`POST /query/`）：基于检索片段生成答案，附带原文引用。系统 Prompt 角色设定为票据业务专家（20年经验）。支持问候词识别（不触发检索）和无关咨询识别（礼貌拒绝）。

**增强版问答**（`POST /query/v2`）：在标准问答基础上增加：
- **意图路由**：关键词快速匹配（<1ms）→ LLM 语义分类（~200ms），识别 8 个票据业务场景
- **质量评估**：检索不足时触发转人工，未命中查询写入 `search_miss_logs`
- **票据上下文**：可传入 `bill_record_id`，将已入库票据要素注入检索 query

**涉及核心文件**：[app/services/rag_service.py](app/services/rag_service.py)

---

### 3.5 流式问答

基于 SSE（Server-Sent Events）逐 token 推流，前端实时渲染。推流结束后附加引用来源列表。中间件层已配置 `Content-Encoding: identity` 禁止 GZip 缓冲，确保 SSE 实时性。

**涉及核心文件**：[app/api/routers.py](app/api/routers.py)（`POST /query/stream`）

---

### 3.6 票据要素识别

支持两种调用方式：
- **独立识别**（`POST /api/v1/bill-recognition/recognize`）：不写库，同步返回识别结果，适合前端快速预览
- **生命周期入库**（`POST /api/v1/bills/upload`）：识别后写入 BillRecord + BillVersion + Milvus

一个 PDF 可含多张票据，逐页识别。提取 **18 个结构化要素**（基础 15 个 + 扩展 3 个：贸易背景/承兑条款/特殊记载）。

**识别流程**：文件类型判断 → PDF 逐页渲染 → Qwen-VL 视觉大模型提取 → BillElementSchema → 金额大小写一致性校验

**涉及核心文件**：[app/services/bill_recognition.py](app/services/bill_recognition.py)

---

### 3.7 票据生命周期管理

首次上传创建 BillRecord（版本1），再次上传同一票号时追加 BillVersion 并比对背书人变化。自动检测金额/日期矛盾并写入 `risk_flags`。支持分页查询所有票据（`GET /api/v1/bills/`）和按票号查询完整版本历史（`GET /api/v1/bills/{ticket_number}`）。

**涉及核心文件**：[app/services/bill_lifecycle.py](app/services/bill_lifecycle.py)

---

### 3.8 意图路由与专项检索

识别 8 个票据业务场景（贴现定价/背书转让/风险识别/法规合规/业务流程/票据查询/纠纷处理/监管政策）。两阶段串联：关键词快速匹配（< 1ms）→ LLM 语义分类（~200ms）。

**涉及核心文件**：[app/services/intent_router.py](app/services/intent_router.py)

---

### 3.9 检索质量评估与转人工

三档质量分：`SUFFICIENT`（≥0.7）/ `PARTIAL`（0.4~0.7）/ `INSUFFICIENT`（<0.4）。`INSUFFICIENT` 时触发转人工，未命中查询写入 `search_miss_logs`。

---

### 3.10 租户管理

创建租户（统一社会信用代码去重），查看租户列表（`GET /api/v1/tenants/`）和统计（文档数/分块数/处理成功率/查询量/平均耗时）。`super_admin` 可查全部租户，`tenant_admin` 只能查本租户。

---

### 3.11 系统监控

Prometheus 指标采集 + Grafana 可视化。监控项：查询次数、检索耗时分布、LLM 耗时、分块总数、限流次数。

**涉及核心文件**：[app/services/metrics.py](app/services/metrics.py)、[docker/prometheus.yml](docker/prometheus.yml)

---

### 3.12 前端管理后台

原生 HTML/CSS/JavaScript，无框架依赖。包含 7 个页面：
- **登录页**（index.html）：JWT 登录
- **聊天问答页**（chat.html）：SSE 流式输出 + 引用来源展示 + **票据咨询审核报告（新）**
- **仪表板**（admin/dashboard.html）：租户概览统计
- **文档管理**（admin/documents.html）：上传/列表/删除/状态轮询
- **租户管理**（admin/tenants.html）：仅管理员可见
- **检索分析**（admin/analytics.html）：意图分布/命中率/耗时趋势
- **系统监控**（admin/monitoring.html）：内嵌 Grafana 面板

#### 聊天页票据操作三种模式（新增票据咨询）

点击输入框左侧 **+** 按钮弹出附件菜单，支持三种操作：

| 菜单项 | 行为 | 后端接口 |
|--------|------|----------|
| **票据合规咨询（新）** | 选择票据文件后显示"文件芯片"，用户继续输入问题后按 Enter，系统将文件+问题一起提交，返回结构化审核报告 | `POST /api/v1/query/consult`（multipart） |
| **票据要素识别** | 立即调用 OCR/VL 识别，以字段卡片形式展示 18 个要素 | `POST /api/v1/bill-recognition/recognize` |
| **票据入库管理** | 将票据入库并触发生命周期管理，返回版本号及风险标记 | `POST /api/v1/bills/upload` |

#### 审核报告卡片展示

票据咨询（`response_type === "agent_report"`）时，AI 气泡内渲染结构化报告卡片：
- **结论徽章**：可执行（绿）/ 不可执行（红）/ 需关注（黄）/ 审核中（蓝紫）
- **风险级别**：LOW → CRITICAL 六档渐变色标签
- **评分进度条**：0~100 分渐变色横条
- **问题列表**：按 error / warning / info 三级分色显示各违规字段和建议
- **摘要文本**：LLM 生成的自然语言总结
- **报告底部**：任务 ID + 耗时

若意图识别为知识查询（无 Agent 触发），则降级展示普通 RAG 回答（带来源按钮）。

### 3.13 多层次业务缓存（app/core/cache.py）

Redis 业务缓存层，独立于限流模块（rate_limiter.py），专门缓存各层计算结果以降低响应时间和 LLM Token 消耗。

#### 票据指纹机制

缓存命中的核心判断依据——**必须保证票据所有要素完全一致**：

```
ticket_type + ticket_number + issue_date + due_date + amount_numeric + amount_text
+ currency + drawer + drawer_account + drawer_bank + acceptor + payee + drawee_bank
+ endorsers(list) + maturity_days + trade_purpose + acceptance_clause + special_remarks
```

将以上 18 个字段 JSON 序列化后取 SHA-256 前 24 位（96 bit），仅票号相同但其他要素不同的票据**不会**命中同一缓存。

#### 五层缓存 Key 设计

| 层次 | Redis Key 格式 | TTL | 覆盖范围 |
|------|--------------|-----|---------|
| 1（最高） | `audit_cache:{tenant}:{bill_fp}:{task_type}` | 4h | 整条多智能体流水线（命中后直接返回历史 task_id） |
| 2 | `compliance:{tenant}:{bill_fp}` | 6h | 18 字段 × RAG 合规检索（最贵操作，节省 18 次 LLM 调用） |
| 2 | `fraud:{tenant}:{bill_fp}` | 2h | 五维欺诈检测评分 |
| 3 | `rag:{tenant}:{query_fp}` | 2h | RAG 问答（检索 + LLM 生成） |
| 4 | `intent:{query_fp}` | 24h | 意图识别（仅 LLM 通道，关键词通道不缓存） |
| 5 | `recognition:{file_md5}` | 24h | 票据图片视觉识别（同文件只调用一次 Qwen-VL） |

#### 降级保证

Redis 不可用时所有缓存操作静默跳过，业务流程零感知。`CACHE_ENABLED=false` 可一键禁用缓存（用于调试或强制全量刷新）。

---

## 4. 多智能体合规审核系统（Agent 层）

### 4.1 Agent 系统架构

#### 三层架构设计

```
┌─────────────────────────────────────────────────────────┐
│  层一：MCP 工具层（app/mcp/）                             │
│  11 个 @mcp.tool() 函数 ← FastMCP 实例注册               │
│  对外：/mcp HTTP 端点（供 Claude API 等外部 LLM 调用）     │
│  对内：LangGraph 节点进程内直接 import 调用（零网络开销）   │
├─────────────────────────────────────────────────────────┤
│  层二：LangGraph 执行引擎（app/graph/）                   │
│  BillAuditState（TypedDict） → PostgresSaver 持久化       │
│  8 种业务 StateGraph（对应 8 种业务类型）                  │
│  条件边：关键节点失败 → END，并行节点：asyncio.gather       │
├─────────────────────────────────────────────────────────┤
│  层三：专项 Agent 业务逻辑层（app/agents/）               │
│  11 个 Agent.run() — 完全不感知 MCP/LangGraph            │
│  业务逻辑独立封装，可单独测试和复用                        │
└─────────────────────────────────────────────────────────┘
```

#### 11 个 MCP 工具 ↔ 专项 Agent 对应关系

| MCP 工具函数 | 对应 Agent | 模块 | 核心职责 |
|------------|-----------|------|---------|
| `parse_bill_document` | `DocumentParserAgent` | M3 | 封装 pdf_parser，提取文本，置信度 < 0.85 产生警告 |
| `extract_bill_elements` | `ElementExtractionAgent` | M3 | 封装 bill_recognition，提取并扩展为 18 个要素字段 |
| `check_compliance` | `ComplianceRetrievalAgent` | M4 | 18 字段 × 并行 RAG 查询，每字段对应法规条文引用 |
| `analyze_endorsement_chain` | `EndorsementChainAgent` | M4 | 有向图重建 + DFS 环路检测，9 类违规检测 |
| `detect_fraud` | `FraudDetectionAgent` | M8 | 五维 asyncio.gather 并行检测，重复票据强制置 1.0 |
| `review_contract` | `ContractReviewAgent` | M5 | 票据要素与合同 6 项比对，贸易背景真实性评分 |
| `assess_risk` | `RiskAssessmentAgent` | M5 | 四维加权评分，五档风险等级判定 |
| `generate_report` | `ReportGenerationAgent` | M6 | 聚合 9 节 JSON，reportlab 渲染 PDF 审核报告 |
| `check_bill_issuance` | `BillIssuanceAgent` | M10 | 出票三项预检：要素完整性 + 黑名单 + 授信系统 |
| `track_bill_flow` | `FlowTrackingAgent` | M7 | 七步报文状态机，适配器模式隔离外部接口 |
| `schedule_batch_audit` | `BatchSchedulingAgent` | M9 | Semaphore(100) 控制并发，子任务失败隔离 |

> **OrchestratorAgent** 作为编排入口，使用 LangGraph StateGraph 替代原自研 DAGExecutor；不直接对应单一 MCP 工具，由 `/api/v1/audit/tasks` 路由调用。

#### 8 种业务 LangGraph StateGraph 配置

| 业务类型 | 英文标识 | LangGraph 执行拓扑 |
|---------|---------|------------|
| 全流程审核 | `FULL_AUDIT` | parse→extract→parallel_cef（compliance+endorse+fraud）→contract（可选）→risk→report |
| 出票预检 | `ISSUANCE_CHECK` | parse→extract→parallel_ci（compliance+issuance）→risk→report |
| 贴现申请 | `DISCOUNT_APPLY` | parse→extract→parallel_cef（compliance+endorse+fraud）→contract（**关键**）→risk→report |
| 提示承兑 | `ACCEPTANCE_PROMPT` | parse→extract→parallel_ce（compliance+endorse）→risk→report |
| 背书转让 | `ENDORSEMENT` | parse→extract→parallel_ef（endorse+fraud）→risk→report |
| 提示付款 | `PAYMENT_PROMPT` | parse→extract→parallel_cef（同 FULL_AUDIT）→contract（可选）→risk→report |
| 质押融资 | `PLEDGE` | parse→extract→parallel_cef（同 FULL_AUDIT）→contract（可选）→risk→report |
| 托收代收 | `COLLECTION` | parse→extract→parallel_cef（同 FULL_AUDIT）→contract（可选）→risk→report |

> `parallel_*` 节点内部使用 `asyncio.gather` 并发调用多个 MCP 工具，等全部完成后由条件边决定是否继续。关键节点失败通过 `failed_nodes` 字段传播，条件边自动路由到 `END`。

#### AgentRegistry（Agent 注册表）

[app/agents/utils/agent_registry.py](app/agents/utils/agent_registry.py) 维护 `agent_name → 类` 的全局映射，支持懒加载（首次访问时才 import），OrchestratorAgent 和 API 路由按名称动态实例化 Agent，无需硬编码 import。

---

### 4.2 BaseAgent 基础框架

```python
# 上下文对象：贯穿所有 Agent 的数据载体
@dataclass
class AgentContext:
    audit_task_id: str       # 主任务 ID
    tenant_id: str           # 租户 ID
    bill_record_id: Optional[str] = None
    document_id: Optional[str] = None
    shared_data: dict = field(default_factory=dict)  # Agent 间共享中间结果
    # 约定键名：
    #   "parsed_doc"         → DocumentParserAgent 输出
    #   "bill_element"       → ElementExtractionAgent 输出（18 字段）
    #   "compliance_summary" → ComplianceRetrievalAgent 汇总
    #   "endorsement_result" → EndorsementChainAgent 输出
    #   "contract_result"    → ContractReviewAgent 输出
    #   "fraud_result"       → FraudDetectionAgent 输出
    #   "risk_result"        → RiskAssessmentAgent 输出
    #   "report_result"      → ReportGenerationAgent 输出
    #   "flow_result"        → FlowTrackingAgent 输出
    #   "issuance_result"    → BillIssuanceAgent 输出

# 结果对象：统一的 Agent 输出格式
@dataclass
class AgentResult:
    agent_name: str
    success: bool
    data: Any = None
    error_code: Optional[str] = None
    error_msg: Optional[str] = None
    elapsed_ms: float = 0.0
```

**BaseAgent.execute()** 统一处理：计时 + 日志（`[{agent_name}] start/done | task={id} elapsed={ms}ms`）+ 异常兜底（子类不处理异常）

**涉及核心文件**：[app/agents/base_agent.py](app/agents/base_agent.py)

---

### 4.3 OrchestratorAgent（任务编排）

已升级为基于 **LangGraph StateGraph** 的执行引擎，彻底替代原自研 DAGExecutor：

```
OrchestratorAgent.run(ctx, db)
    │
    ├── build_audit_graph(task_type, checkpointer=get_checkpointer())
    │   → 返回按业务类型编译好的 LangGraph CompiledGraph
    │
    ├── config = {"configurable": {"thread_id": ctx.audit_task_id}}
    │   （thread_id 与 audit_tasks 表主键对应，实现断点续跑）
    │
    ├── async for event in graph.astream(initial_state, config):
    │   ├── 每完成一个节点推送 event（node_name → delta）
    │   └── _update_progress_from_event()：更新 audit_tasks.progress_pct
    │
    └── final_state = await graph.aget_state(config)
        _sync_state_to_context(final_state, ctx)  ← 向后兼容 ctx.shared_data
```

**状态外置设计**：`BillAuditState` 每步通过 `AsyncPostgresSaver` 写入 PostgreSQL 检查点表。容器重启不丢失执行进度；K8s 滚动更新期间，下一个副本可接手未完成的任务继续执行。

**关键节点失败传播**：`_CRITICAL_NODES_BY_TYPE` 类字典按业务类型定义关键节点集合（使用 Agent 内部名，如 `"element_extraction"`）。`failed_nodes` 字段记录失败节点名，条件边函数根据此字段决定路由。

**涉及核心文件**：[app/agents/orchestrator_agent.py](app/agents/orchestrator_agent.py)、[app/graph/builder.py](app/graph/builder.py)、[app/graph/state.py](app/graph/state.py)

---

### 4.4 M3 解析与识别层

#### DocumentParserAgent
- 封装 `BillPDFParser`，使用 `asyncio.to_thread` 将同步解析放到线程池
- 置信度阈值 0.85，低于阈值在结果中附加 `low_confidence` 警告标志
- 写入 `shared_data["parsed_doc"]`

#### ElementExtractionAgent
- 封装 `BillRecognitionService`，提取并扩展为 **18 个字段**
- 3 个扩展字段：`trade_purpose`（贸易背景）、`acceptance_clause`（承兑条款）、`special_remarks`（特殊记载）
- 通过关键词匹配从 OCR 原始文本中提取扩展字段
- 写入 `bill_elements` 表 + `shared_data["bill_element"]`

**涉及核心文件**：[app/agents/document_parser_agent.py](app/agents/document_parser_agent.py)、[app/agents/element_extraction_agent.py](app/agents/element_extraction_agent.py)

---

### 4.5 M4 合规检索层

#### ComplianceRetrievalAgent
- 为 18 个字段分别构建合规查询（含字段值上下文），格式如：
  `"票据号码格式和唯一性要求是什么？（当前值：SHBH20240115）"`
- 使用 `asyncio.gather` 并行发起 18 个 RAG 查询
- 每字段对应一条 `compliance_checks` 表记录
- 合规判断规则：
  - 必填字段为空 → `SEVERE` 违规
  - `trade_purpose` 为空 → `WARNING` 违规
  - RAG 检索分数 ≥ 0.70 且字段有值 → 合规

#### EndorsementChainAgent
9 类背书违规检测（EN01~EN09 全部实现）：

| 代码 | 违规类型 | 检测方法 |
|------|---------|---------|
| EN01 | 背书链断裂 | 收款人与前手背书人不一致 |
| EN02 | 重复背书 | 同一主体出现两次（set 大小比较）|
| EN03 | 背书闭环 | DFS 三色着色算法检测有向图环路 |
| EN04 | 空白背书 | 背书人列表含空值/None |
| EN05 | 背书日期异常 | 背书日期早于前手背书日期 |
| EN06 | 超出最大层数 | 背书人数量 > 10 |
| EN07 | 背书人=出票人 | 出票人名称在背书人列表中 |
| EN08 | 背书撤销后仍流转 | 已撤销背书后仍有流转记录 |
| EN09 | 自我背书 | 相邻背书人相同 |

**涉及核心文件**：[app/agents/compliance_retrieval_agent.py](app/agents/compliance_retrieval_agent.py)、[app/agents/endorsement_chain_agent.py](app/agents/endorsement_chain_agent.py)

---

### 4.6 M5 风险评估层

#### ContractReviewAgent
- 从合同文本中提取：金额（正则）、甲乙方（关键词定位）、用途（关键词定位）
- 6 项比对结果：
  - `amount_match`：票面金额与合同金额误差 ≤ 5%
  - `party_match`：出票人/收款人与合同甲乙方匹配
  - `date_match`：出票日期在合同有效期内
  - `purpose_match`：贸易背景关键词重叠检测
- 无合同时降级为默认 50 分（不阻断审核）

#### RiskAssessmentAgent

**四维加权评分公式**：
```
综合评分 = 合规维度 × 0.30
         + 背书维度 × 0.30
         + 合同维度 × 0.20
         + 欺诈维度 × 0.20
```

**五档风险阈值**：

| 综合评分 | 风险等级 | 建议处理 |
|---------|---------|---------|
| ≥ 85 | LOW | 自动通过 |
| 70~84 | MEDIUM_LOW | 快速人工审核 |
| 50~69 | MEDIUM_HIGH | 全面人工复核 |
| 30~49 | HIGH | 建议拒绝 |
| < 30 | CRITICAL | 强制拒绝 |

**缺失维度降级分**：合规 60 / 背书 70 / 合同 50 / 欺诈 80（不阻断流程）

**涉及核心文件**：[app/agents/contract_review_agent.py](app/agents/contract_review_agent.py)、[app/agents/risk_assessment_agent.py](app/agents/risk_assessment_agent.py)

---

### 4.7 M6 报告生成层

#### ReportGenerationAgent
- 聚合所有 Agent 的 `shared_data` 输出，构建 **9 节 JSON 结构**：
  `summary` / `elements` / `compliance` / `endorsement` / `contract` / `risk` / `fraud` / `flow` / `conclusion`
- 根据 `risk_level` 自动生成审核决策（APPROVE / APPROVE_WITH_REVIEW / MANUAL_REVIEW / REJECT / MANDATORY_REJECT）
- 使用 `asyncio.to_thread` 将同步 PDF 渲染放到线程池，避免阻塞事件循环
- PDF 渲染失败不阻断流程（JSON 报告仍保存到数据库）
- 支持培训模式（`training_mode=True`）：在 PDF 中附加评分原理注解页

#### ReportRenderer（reportlab 渲染器）
- A4 页面，中英文双语标题
- 风险等级颜色高亮（LOW=绿 #27AE60 / MEDIUM_LOW=橙 / HIGH=红 / CRITICAL=紫 #8E44AD）
- 封面信息表格 + 7 个内容节 + 可选培训注解页
- 使用内置 Helvetica 字体（无中文字体外部依赖）

**涉及核心文件**：[app/agents/report_generation_agent.py](app/agents/report_generation_agent.py)、[app/agents/utils/report_renderer.py](app/agents/utils/report_renderer.py)

---

### 4.8 M7 流转追踪层

#### FlowTrackingAgent
七步报文流转协议（以提示承兑为例）：

| 步骤 | 发送节点 | 应答节点 |
|------|---------|---------|
| 1 | 发起行-报文组装 | 发起行-前置机发送 |
| 2 | 前置机-票交所发送 | 票交所-接收确认 |
| 3 | 票交所-报文处理 | 票交所-转发对手行 |
| 4 | 对手行-接收 | 对手行-处理中 |
| 5 | 对手行-应答生成 | 对手行-票交所应答 |
| 6 | 票交所-应答接收 | 票交所-发起行转发 |
| 7 | 发起行前置机-应答接收 | 发起行-业务完成 |

**超时预警五级**：NORMAL（<30s）/ WATCH（30~60s）/ WARNING（60~120s）/ URGENT（120~300s）/ OVERDUE（>300s）

**Mock 适配层**（[app/agents/utils/flow_channel_mock.py](app/agents/utils/flow_channel_mock.py)）：
- 模拟前置机/票交所/对手行三个外部接口
- 通过配置 `FLOW_TRACKING_MOCK=true` 切换，生产环境只需替换适配器实现，不改动 Agent 逻辑
- Mock 层按真实协议格式返回数据，各步骤有可配置的失败率和延迟范围

**涉及核心文件**：[app/agents/flow_tracking_agent.py](app/agents/flow_tracking_agent.py)

---

### 4.9 M8 欺诈检测层

#### FraudDetectionAgent

**五维并行检测**（每维 2 秒超时，超时得分归零不阻断）：

| 维度 | 权重 | 检测方法 | 特殊规则 |
|------|------|---------|---------|
| 印章真伪 | 30% | OCR 置信度 + 黑名单向量比对 | — |
| **重复票据** | 25% | PostgreSQL 精确查询历史票据 | **命中强制综合分 = 1.0** |
| 图像篡改 | 25% | OCR 置信度分布分析（CNN 模拟）| — |
| 关联网络 | 15% | 背书链闭环检测（EndorsementChain 结果复用）| — |
| 历史黑名单 | 5% | blacklist_entities 模糊匹配 | — |

**四档欺诈等级**：clean（<0.3）/ suspicious（0.3~0.6）/ high_risk（0.6~0.8）/ fraud（≥0.8）

**涉及核心文件**：[app/agents/fraud_detection_agent.py](app/agents/fraud_detection_agent.py)

---

### 4.10 M9 批量调度层

#### BatchSchedulingAgent
- `asyncio.Semaphore(concurrency)` 控制并发槽位（默认 10，最大 100）
- 每个子任务使用独立 `AgentContext`（状态完全隔离，互不影响）
- 子任务异常被捕获，写入 `batch_task_items.error_msg`，不影响其他子任务继续执行
- 批量失败率 ≥ 50% 时整批标记为 `FAILED`；否则为 `COMPLETED`
- `subtask_runner` 可通过构造函数注入替换（测试友好设计）

**涉及核心文件**：[app/agents/batch_scheduling_agent.py](app/agents/batch_scheduling_agent.py)

---

### 4.11 M10 出票预检

#### BillIssuanceAgent
三项并行预检（`asyncio.gather`）：

1. **要素完整性**：8 个出票必填字段非空 + 金额范围（1万~5亿）+ 期限 ≤ 365天
2. **黑名单查询**：出票人/承兑人/收款人在 `blacklist_entities` 中模糊匹配
3. **授信系统**：出票金额不超过授信额度（Mock 实现，生产替换为 HTTP 调用，通过 `CREDIT_SYSTEM_MOCK` 开关控制）

预检结果写入 `shared_data["issuance_result"]` 和 `shared_data["issuance_result"]`（供 OrchestratorAgent 决定是否继续执行后续节点）。

**两种使用场景**：
- **实时预检**（`POST /audit/issuance/check`）：秒级响应，适合出票表单填写时即时校验，不创建 AuditTask 记录
- **出票正式审核**（`POST /audit/tasks` + `task_type=issuance_check`）：走 OrchestratorAgent 完整链路，有完整报告和 DB 记录

**涉及核心文件**：[app/agents/bill_issuance_agent.py](app/agents/bill_issuance_agent.py)

---

## 5. 数据库设计

### 5.1 原有 PostgreSQL 表（RAG 基础层）

| 表名 | 用途 |
|------|------|
| `tenants` | 租户注册信息、配额、Milvus 分区键 |
| `users` | 用户账号（bcrypt 密码、JWT 角色） |
| `documents` | 文档主档（状态、分块数、MD5 去重、批次关联）|
| `document_chunks` | 文档片段（Milvus ID、章节路径、token 数）|
| `bill_records` | 票据主档（票号唯一键、18 要素、风险标记）|
| `bill_versions` | 票据流转版本（背书人变化记录，每次上传追加一版）|
| `query_logs` | 问答日志（耗时、评分、引用 chunks、意图类型）|
| `intent_logs` | 意图路由日志（路由决策与耗时）|
| `search_miss_logs` | 未命中查询（知识库补充优先级分析）|

### 5.2 新增 14 张表（Agent 层）

| 表名 | 对应 Agent | 关键字段 |
|------|-----------|---------|
| `audit_tasks` | OrchestratorAgent | status, task_type, dag_plan(JSON), progress_pct, current_agent |
| `bill_elements` | ElementExtractionAgent | 18 个要素字段, confidence_score, raw_ocr_result |
| `compliance_checks` | ComplianceRetrievalAgent | element_field, regulation_ref, is_compliant, violation_level, rag_query/answer/score |
| `endorsement_chains` | EndorsementChainAgent | chain_graph(JSON), is_continuous, has_cycle, violation_codes(JSON) |
| `contract_reviews` | ContractReviewAgent | match_score, trade_background_score, amount_match, party_match, mismatch_details |
| `risk_assessments` | RiskAssessmentAgent | compliance/endorsement/contract/fraud_score, composite_score, risk_level, missing_dimensions |
| `audit_reports` | ReportGenerationAgent | report_json(JSON-9节), pdf_path, pdf_size_bytes, is_final, training_mode |
| `flow_tracking_tasks` | FlowTrackingAgent | business_type, total_steps=7, completed_steps, timeout_level, summary_text |
| `flow_messages` | FlowTrackingAgent | step_index(1-7), node_name, msg_type, status, processing_ms, is_anomaly |
| `fraud_detections` | FraudDetectionAgent | seal/duplicate/tamper/network/blacklist_score, overall_fraud_score, fraud_level |
| `batch_tasks` | BatchSchedulingAgent | total_count, completed_count, concurrency, result_summary(JSON) |
| `batch_task_items` | BatchSchedulingAgent | batch_task_id, item_index, status, error_msg |
| `error_code_mappings` | 系统通用 | error_code(UNIQUE), severity, zh_title, zh_desc, remediation |
| `blacklist_entities` | FraudDetectionAgent/BillIssuanceAgent | entity_type, entity_name, is_active, expired_at |

所有新表定义在 [app/models/agent_models.py](app/models/agent_models.py)，继承自原 `db_models.py` 的 `Base`，不改动原有表结构。

### 5.3 Milvus 向量集合

#### 集合名：`bill_documents`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | VARCHAR(100) PK | 片段唯一 ID，格式 `{document_id}_{chunk_index}` |
| `tenant_id` | VARCHAR(50) PARTITION_KEY | 租户隔离，A 租户完全看不到 B 租户向量 |
| `document_id` | VARCHAR(100) | 所属文档 ID，删除文档时批量清除其所有向量 |
| `chunk_index` | INT64 | 片段在文档中的序号（0-based） |
| `content` | VARCHAR(65535) | 片段文本，检索命中后作为 LLM 上下文 |
| `section_path` | VARCHAR(500) | 章节路径，用于答案溯源展示 |
| `page_num` | INT64 | 原文档起始页码 |
| `chunk_type` | VARCHAR(50) | 片段类型：text / table / image_ocr |
| `md5_hash` | VARCHAR(32) | 文件 MD5，幂等防重入库 |
| `dense_vector` | FLOAT_VECTOR(1024) | BGE-M3 稠密向量（HNSW 索引） |
| `sparse_vector` | SPARSE_FLOAT_VECTOR | BGE-M3 稀疏向量（SPLADE） |

---

## 6. API 接口总览

### RAG 基础层

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/v1/auth/login` | 公开 | 用户登录，返回 JWT |
| POST | `/api/v1/auth/register` | tenant_admin+ | 注册用户 |
| POST | `/api/v1/documents/upload` | tenant_admin+ | 上传单个文档（后台异步入库）|
| POST | `/api/v1/documents/upload/batch` | tenant_admin+ | 批量上传（失败级联取消整批）|
| POST | `/api/v1/documents/upload/folder` | tenant_admin+ | 服务器本地文件夹批量入库 |
| GET  | `/api/v1/documents/` | 已登录 | 文档列表（分页+状态过滤）|
| GET  | `/api/v1/documents/stats` | 已登录 | 分块总数、今日查询数统计 |
| GET  | `/api/v1/documents/batch/{batch_id}` | 已登录 | 批次内所有文件处理状态 |
| POST | `/api/v1/documents/batch/{batch_id}/retry` | tenant_admin+ | 重试批次内所有失败文件 |
| GET  | `/api/v1/documents/{id}` | 已登录 | 单文档详情 |
| POST | `/api/v1/documents/{id}/retry` | tenant_admin+ | 重新触发失败文档入库 |
| DELETE | `/api/v1/documents/{id}` | tenant_admin+ | 删除文档（清除 Milvus 向量）|
| POST | `/api/v1/query/` | 已登录 | 标准同步问答 |
| POST | `/api/v1/query/stream` | 已登录 | 流式问答（SSE）|
| POST | `/api/v1/query/v2` | 已登录 | 增强版问答（意图路由+质量评估+票据上下文）|
| **POST** | **`/api/v1/query/consult`** | 已登录 | **票据咨询（multipart：文件+问题→意图路由→审核报告或知识答案）** |
| GET  | `/api/v1/query/stats` | 已登录 | 检索统计（专项/模糊×成功/失败四维度）|
| POST | `/api/v1/query/feedback` | 已登录 | 提交评分（1~5 星）|
| POST | `/api/v1/bill-recognition/recognize` | 已登录 | 票据识别（不写库，同步返回）|
| POST | `/api/v1/bills/upload` | 已登录 | 票据上传+生命周期入库 |
| GET  | `/api/v1/bills/` | 已登录 | 票据列表（分页）|
| GET  | `/api/v1/bills/{ticket_number}` | 已登录 | 票据详情（含版本历史）|
| POST | `/api/v1/tenants/` | super_admin | 创建租户 |
| GET  | `/api/v1/tenants/` | tenant_admin+ | 租户列表（admin 查全部，tenant_admin 查本租户）|
| GET  | `/api/v1/tenants/{id}/stats` | tenant_admin+ | 租户统计 |
| GET  | `/metrics` | 公开 | Prometheus 指标 |
| GET  | `/health` | 公开 | 健康检查（含 Milvus/Redis 依赖状态）|

### Agent 层（已实现并注册）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/audit/tasks` | 提交审核任务（后台异步，立即返回 task_id）|
| GET  | `/api/v1/audit/tasks/{id}` | 查询审核进度（status / progress_pct / current_agent）|
| GET  | `/api/v1/audit/tasks/{id}/report` | 下载 JSON 报告（9 节结构）|
| GET  | `/api/v1/audit/tasks/{id}/report/pdf` | 下载 PDF 报告（文件流）|
| POST | `/api/v1/audit/issuance/check` | 出票预检（同步，秒级响应，不创建审核任务记录）|
| POST | `/api/v1/batch/tasks` | 创建批量审核任务（后台异步，立即返回 batch_id）|
| GET  | `/api/v1/batch/tasks/{batch_id}` | 批量进度查询（completed/failed/failure_rate）|
| GET  | `/api/v1/batch/tasks/{batch_id}/items` | 批次子项列表（分页，含每个文件的状态和错误信息）|
| POST | `/api/v1/tracking/tasks` | 创建流转追踪任务（同步执行 7 步，返回完整结果）|
| GET  | `/api/v1/tracking/tasks/{id}` | 查询七步流转整体状态 |
| GET  | `/api/v1/tracking/tasks/{id}/messages` | 报文节点详情（14 条记录，含耗时和异常标记）|

---

## 7. 测试覆盖

### Agent 层测试套件

```
tests/test_agents/
├── test_m1_models.py        # 14 张表结构 + BaseAgent（27 个测试）
├── test_m2_orchestrator.py  # OrchestratorAgent + LangGraph 流式执行（22 个测试）
├── test_m3_parsing.py       # 文档解析 + 要素提取 18 字段（16 个测试）
├── test_m4_compliance.py    # 18 并行 RAG + 9 类背书违规（18 个测试）
├── test_m5_risk.py          # 合同 6 项比对 + 五档风险阈值（34 个测试）
├── test_m6_report.py        # 9 节 JSON + PDF 文件生成（15 个测试）
├── test_m7_flow.py          # 七步状态机 + 五级超时预警（19 个测试）
├── test_m8_fraud.py         # 五维并行检测 + 重复票据强制规则（22 个测试）
├── test_m9_batch.py         # Semaphore 并发上限 + 失败隔离（19 个测试）
└── test_m10_api.py          # 出票预检 + 端到端链路集成（17 个测试）
```

### MCP 工具层测试套件

```
tests/test_mcp/
└── test_mcp_tools.py        # 11 个工具注册验证 + JSON Schema + Mock Agent 调用（22 个测试）
```

### LangGraph 执行引擎测试套件

```
tests/test_graph/
├── test_state.py            # BillAuditState JSON 序列化 + 字段隔离（17 个测试）
├── test_nodes.py            # 11 个节点函数 + 并行节点 + 失败传播（30 个测试）
└── test_builder.py          # 8 种业务图编译 + 条件边路由 + 并行层执行（23 个测试）
```

### 可观测性测试套件

```
tests/test_observability/
└── test_tracing.py          # Trace ID 基础逻辑 + 50并发协程隔离 + 中间件 + 健康检查（19 个测试）
```

**总计：325 个测试，全部通过**

### 运行方式

```bash
# 激活项目环境
conda activate bill_rag_env

# 运行全部测试
python -m pytest tests/ -v

# 按套件运行
python -m pytest tests/test_agents/ -v           # Agent 层（209 个）
python -m pytest tests/test_mcp/ -v              # MCP 工具层（22 个）
python -m pytest tests/test_graph/ -v            # LangGraph 引擎（70 个）
python -m pytest tests/test_observability/ -v   # 可观测性（19 个）

# 运行单模块测试
python -m pytest tests/test_graph/test_builder.py -v

# 初始化数据库表 + 插入种子数据（每张 Agent 表 100 条）
python scripts/seed_agent_data.py

# 初始化超级管理员账号
python scripts/init_admin.py
```

---

## 8. 部署架构

```
┌────────────────────────────────────────────────────────────────┐
│                    Docker Compose 网络                          │
│  ┌────────────────┐   ┌────────────────┐   ┌────────────────┐ │
│  │  bill_rag_app  │   │bill_rag_postgres│   │ bill_rag_redis │ │
│  │  Port: 8000    │   │  Port: 5432    │   │  Port: 6379    │ │
│  │  FastAPI       │   │  PostgreSQL 16 │   │  Redis 7       │ │
│  └────────────────┘   └────────────────┘   └────────────────┘ │
│  ┌────────────────┐   ┌────────────────────────────────────┐  │
│  │bill_rag_grafana│←──│       bill_rag_prometheus          │  │
│  │  Port: 3000    │   │         Port: 9090                 │  │
│  └────────────────┘   └────────────────────────────────────┘  │
│  外部共享网络 milvus_default                                     │
│  ┌────────────────┐                                             │
│  │ Milvus 向量库  │  Port: 19530（与其他项目共享）               │
│  └────────────────┘                                             │
└────────────────────────────────────────────────────────────────┘
```

### 快速启动

```bash
# 1. 复制并填写环境变量
cp .env.example .env

# 2. 启动全部服务（首次启动会自动建表）
docker compose -f docker/docker-compose.yml up -d

# 3. 初始化管理员账号
python scripts/init_admin.py

# 4. 插入 Agent 层种子数据
conda activate bill_rag_env && python scripts/seed_agent_data.py

# 5. 访问 API 文档
open http://localhost:8000/api/docs
```

### 关键环境变量

```ini
# 数据库
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/bill_rag

# 安全
SECRET_KEY=your-secret-key-min-32-chars
ACCESS_TOKEN_EXPIRE_MINUTES=60

# 向量库
MILVUS_HOST=localhost
MILVUS_PORT=19530

# 缓存
REDIS_URL=redis://localhost:6379/0

# 模型路径（本地部署）
BGE_M3_MODEL_PATH=/path/to/BAAI/bge-m3
BGE_RERANKER_PATH=/path/to/BAAI/bge-reranker-v2-m3

# 大模型 API
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o
VISION_MODEL=qwen-vl-max

# Agent 层开关
FLOW_TRACKING_MOCK=true       # 流转追踪 Mock 模式（生产改为 false）
CREDIT_SYSTEM_MOCK=true       # 授信系统 Mock 模式（生产改为 false）

# 多层次业务缓存配置（TTL 单位：秒）
CACHE_ENABLED=true            # 全局缓存开关（false 可一键禁用，用于调试）
CACHE_AUDIT_TTL=14400         # 审核任务级缓存：4 小时
CACHE_COMPLIANCE_TTL=21600    # 合规 RAG 检索缓存：6 小时
CACHE_FRAUD_TTL=7200          # 欺诈检测缓存：2 小时
CACHE_RAG_TTL=7200            # RAG 问答缓存：2 小时
CACHE_INTENT_TTL=86400        # 意图识别缓存：24 小时
CACHE_RECOGNITION_TTL=86400   # 票据图片识别缓存：24 小时
```

---

## 9. 目录结构

```
bill_rag/
├── app/
│   ├── api/
│   │   ├── routers.py                  # RAG 基础层路由（认证/文档/问答/租户/票据）+ 增强健康检查
│   │   ├── audit_router.py             # 审核任务路由（提交/查询/报告/出票预检）
│   │   ├── batch_router.py             # 批量审核路由（批次提交/进度查询/子项列表）
│   │   └── tracking_router.py          # 流转追踪路由（创建追踪/查询状态/报文详情）
│   ├── core/
│   │   ├── auth.py                     # JWT + 密码哈希 + 依赖注入
│   │   ├── database.py                 # PostgreSQL 异步连接池
│   │   ├── cache.py                    # 五层业务缓存（票据指纹+Redis异步客户端）
│   │   ├── logging.py                  # loguru 生产级日志配置
│   │   └── tracing.py                  # ContextVar Trace ID（多容器可观测性）
│   ├── models/
│   │   ├── db_models.py                # 原有 9 张表 ORM
│   │   ├── agent_models.py             # 新增 14 张 Agent 表 ORM（枚举+表定义）
│   │   └── schemas.py                  # Pydantic 请求/响应 Schema
│   ├── services/
│   │   ├── ingestion.py                # 文档入库流水线
│   │   ├── pdf_parser.py               # 多格式解析（PDF/Word/Excel/图片）
│   │   ├── embedding.py                # BGE-M3 三路向量化
│   │   ├── vector_store.py             # Milvus 混合检索
│   │   ├── rag_service.py              # RAG 完整流程（标准+增强版）
│   │   ├── bill_recognition.py         # 票据要素视觉识别
│   │   ├── bill_lifecycle.py           # 票据生命周期持久化
│   │   ├── intent_router.py            # 意图路由（8 场景）
│   │   ├── rate_limiter.py             # Redis 滑动窗口限流
│   │   └── metrics.py                  # Prometheus 监控埋点
│   ├── mcp/                            # MCP 工具层（Model Context Protocol）
│   │   ├── __init__.py
│   │   ├── server.py                   # FastMCP 实例 + 所有工具注册 + ASGI 挂载
│   │   └── tools/                      # 11 个工具模块（每 Agent 对应一个）
│   │       ├── __init__.py
│   │       ├── document_tools.py       # parse_bill_document → DocumentParserAgent
│   │       ├── extraction_tools.py     # extract_bill_elements → ElementExtractionAgent
│   │       ├── compliance_tools.py     # check_compliance → ComplianceRetrievalAgent
│   │       ├── endorsement_tools.py    # analyze_endorsement_chain → EndorsementChainAgent
│   │       ├── fraud_tools.py          # detect_fraud → FraudDetectionAgent
│   │       ├── contract_tools.py       # review_contract → ContractReviewAgent
│   │       ├── risk_tools.py           # assess_risk → RiskAssessmentAgent
│   │       ├── report_tools.py         # generate_report → ReportGenerationAgent
│   │       ├── issuance_tools.py       # check_bill_issuance → BillIssuanceAgent
│   │       ├── flow_tools.py           # track_bill_flow → FlowTrackingAgent
│   │       └── batch_tools.py          # schedule_batch_audit → BatchSchedulingAgent
│   ├── graph/                          # LangGraph 执行引擎
│   │   ├── __init__.py                 # setup_checkpointer() + get_checkpointer()
│   │   ├── state.py                    # BillAuditState TypedDict（外置到 PostgreSQL）
│   │   ├── nodes.py                    # 11 个 LangGraph 节点函数 + 5 个并行节点变体
│   │   ├── edges.py                    # 条件边函数（after_parse/after_extract/after_risk 等）
│   │   └── builder.py                  # build_audit_graph()，按业务类型构建 StateGraph
│   ├── agents/
│   │   ├── __init__.py
│   │   ├── base_agent.py               # AgentContext / AgentResult / BaseAgent 抽象基类
│   │   ├── orchestrator_agent.py       # 任务编排：LangGraph astream() + 进度更新
│   │   ├── document_parser_agent.py    # M3：文档解析（封装 pdf_parser）
│   │   ├── element_extraction_agent.py # M3：18 字段要素提取（封装 bill_recognition）
│   │   ├── compliance_retrieval_agent.py # M4：18 字段并行 RAG 合规检索
│   │   ├── endorsement_chain_agent.py  # M4：背书有向图 + 9 类违规检测
│   │   ├── contract_review_agent.py    # M5：合同 6 项要素比对
│   │   ├── risk_assessment_agent.py    # M5：四维加权评分 + 五档风险等级
│   │   ├── report_generation_agent.py  # M6：9 节 JSON 报告 + PDF 渲染
│   │   ├── flow_tracking_agent.py      # M7：七步报文状态机
│   │   ├── fraud_detection_agent.py    # M8：五维并行欺诈检测
│   │   ├── batch_scheduling_agent.py   # M9：Semaphore 批量并发调度
│   │   ├── bill_issuance_agent.py      # M10：出票三项预检
│   │   └── utils/
│   │       ├── __init__.py
│   │       ├── agent_registry.py       # Agent 注册表（名称→类映射，懒加载）
│   │       ├── report_renderer.py      # reportlab PDF 渲染器（A4+风险色彩）
│   │       └── flow_channel_mock.py    # 流转追踪 Mock 适配层（前置机/票交所/对手行）
│   └── main.py                         # FastAPI 应用入口 + 生命周期钩子 + MCP 挂载
├── config/
│   ├── settings.py                     # Pydantic Settings 全局配置
│   └── bill_dict.txt                   # jieba 票据领域词典
├── frontend/
│   ├── index.html                      # 登录页
│   ├── chat.html                       # 聊天问答页（SSE 流式输出）
│   └── admin/                          # 管理后台（仪表板/文档/租户/分析/监控）
├── docker/
│   ├── docker-compose.yml              # 完整服务编排（app+postgres+redis+prometheus+grafana）
│   ├── Dockerfile
│   ├── prometheus.yml
│   └── grafana/                        # Grafana 面板 JSON
├── scripts/
│   ├── init_admin.py                   # 初始化超级管理员账号
│   └── seed_agent_data.py              # 建 14 张 Agent 表 + 每表 100 条种子数据
├── tests/
│   ├── test_agents/                    # Agent 业务逻辑测试（209 个测试）
│   │   ├── test_m1_models.py
│   │   ├── test_m2_orchestrator.py
│   │   ├── test_m3_parsing.py
│   │   ├── test_m4_compliance.py
│   │   ├── test_m5_risk.py
│   │   ├── test_m6_report.py
│   │   ├── test_m7_flow.py
│   │   ├── test_m8_fraud.py
│   │   ├── test_m9_batch.py
│   │   └── test_m10_api.py
│   ├── test_mcp/                       # MCP 工具层测试（22 个测试）
│   │   └── test_mcp_tools.py
│   ├── test_graph/                     # LangGraph 引擎测试（70 个测试）
│   │   ├── test_state.py
│   │   ├── test_nodes.py
│   │   └── test_builder.py
│   └── test_observability/             # 可观测性测试（19 个测试）
│       └── test_tracing.py
├── requirements.txt                    # Python 依赖（含 MCP + LangGraph）
└── .env                                # 环境变量（不提交 Git）
```

---

*文档更新时间：2026-05-26 | 系统版本：v3.0.0（RAG 基础层 + 多智能体 + MCP 工具层 + LangGraph 执行引擎）*
